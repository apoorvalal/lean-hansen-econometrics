# Lean Review Report

Reviewed 2 file(s); 7 confirmed finding(s).

## HansenEconometrics/Chapter10Bootstrap/HigherOrder.lean

### hygiene

- **[major]** `secondOrder_scaled_probability_transfer` (line 24) — This 895-line module exposes 27 public theorems (Hansen Theorem 10.15 and 10.17 second-order Edgeworth bridges plus their transfer/event/polynomial/one-sub-alpha variants) but has no module-level docstring explaining its contents and public surface.
  - rule: AGENTS.md PR-gate large-module-docstring
  - evidence: `wc -l` = 895 lines. `rg '^/-!'` and `rg '/-!'` on the file return no matches (no module header). `rg -nc '^theorem'` = 27; `rg -nc '^private'` = 0, so all 27 top-level theorems are public (default visibility, e.g. secondOrder_scaled_probability_transfer at line 24, chapter10_percentileT_secondOrder_interval_* and chapter10_abs_test_secondOrder_rejection_* families). Lines 17-23 show only a per-theorem `/-- Generic second-order probability transfer. -/` docstring, not a module header. AGENTS.md:18 reads: "- Give large modules a docstring that explains their contents and public surface."
  - suggested fix: Add a `/-! # Chapter 10 higher-order refinements ... -/` module docstring near the top of the file (after the imports/namespace) summarizing the two theorem families (percentile-t interval coverage = Thm 10.15; two-sided abs-test rejection = Thm 10.17), the generic `secondOrder_scaled_probability_transfer` engine, and the `_of_transfer` / `_event_` / `_polynomial` / `_one_sub_alpha` variant naming scheme.
  - mechanical: true | confidence: high

## HansenEconometrics/Chapter10Bootstrap/Quantiles.lean

### redundancy

- **[minor]** `bootstrapScalarCDFIndexed_mono` (line 1328) — bootstrapScalarCDFIndexed_mono re-proves the ENNReal.toReal_mono/measure_mono monotonicity argument from scratch even though bootstrapScalarCDFIndexed is defined as scalarCDF (Pstar n ω) (Zstar n ω), so its goal is definitionally the already-proved scalarCDF_mono and should delegate rather than duplicate that proof block.
  - rule: AGENTS.md §2 reuse-repo-theorems / §4 bridge-lemmas-delegate
  - evidence: def bootstrapScalarCDFIndexed ... := scalarCDF (Pstar n ω) (Zstar n ω) x (lines 1302-1306). scalarCDF_mono proof (lines 326-328): `intro x y hxy; refine ENNReal.toReal_mono (measure_ne_top ν {ωlim | Z ωlim ≤ y}) ?_; exact measure_mono fun ωlim hωlim => le_trans hωlim hxy`. bootstrapScalarCDFIndexed_mono proof (lines 1333-1336): `intro x y hxy; refine ENNReal.toReal_mono (measure_ne_top (Pstar n ω) {ωs | Zstar n ω ωs ≤ y}) ?_; exact measure_mono fun ωs hωs => le_trans hωs hxy` — structurally identical modulo the ν/Z substitution that the def makes definitional. Caller check (`rg -n 'bootstrapScalarCDFIndexed_mono'`): the theorem is referenced only within its own file (Quantiles.lean lines 1814, 1890, 2071) and has no out-of-file callers, so rewriting the proof body to delegate (e.g. `simpa [bootstrapScalarCDFIndexed] using scalarCDF_mono (ν := Pstar n ω) (Z := Zstar n ω)`) is safe — the signature is unchanged.
  - suggested fix: Replace the proof body with a delegation, e.g. `by simpa [bootstrapScalarCDFIndexed] using scalarCDF_mono (ν := Pstar n ω) (Z := Zstar n ω)` (the [IsFiniteMeasure (Pstar n ω)] instance is already in scope).
  - mechanical: false | confidence: high

### hygiene

- **[major]** `Chapter10Bootstrap.Quantiles` (line 1) — This 2114-line module with a very large public surface (80+ public declarations spanning the scalar/indexed bootstrap-quantile API) has no module-level docstring explaining its contents and public surface.
  - rule: AGENTS.md PR-gate docstrings-for-large-modules
  - evidence: `rg '^/-!' HansenEconometrics/Chapter10Bootstrap/Quantiles.lean` returns no matches (exit 1); `rg '/-!' ...` also returns nothing. `wc -l` = 2113 lines. `rg -c '^(theorem|lemma|def|structure|noncomputable def|abbrev|instance) '` = 71. The file opens with `import Mathlib.Probability.CDF` (line 1), `open` directives (lines 5-7), `namespace HansenEconometrics` (line 9), then `section QuantileConvergence` (line 16); the first `/--` is a per-declaration docstring on `CDFQuantileBracket`, not a `/-! -/` module doc. AGENTS.md line 18: "Give large modules a docstring that explains their contents and public surface."
  - suggested fix: Add a `/-! # Bootstrap scalar quantile convergence ... -/` module docstring after the imports summarizing the generalized-inverse, scalarCDF, bootstrapScalarCDF(/Indexed) families and which declarations are the chapter-facing API for Hansen Theorems 10.13/10.14/10.16.
  - mechanical: true | confidence: high
- **[nit]** `lowerCDFQuantile_le` (line 35) — Public helper `lowerCDFQuantile_le` is pure same-file proof scaffolding: it is used exactly once, inside `lowerCDFQuantile_bracket_of_local_right_lt`, has zero out-of-file usages, and is not referenced in the chapter inventory.
  - rule: AGENTS.md public-API keep-same-file-scaffolding-private
  - evidence: `rg -n 'lowerCDFQuantile_le' --glob '*.lean'` returns only two hits, both in HansenEconometrics/Chapter10Bootstrap/Quantiles.lean: line 35 (`theorem lowerCDFQuantile_le`, no `private`) and line 82 (`exact lowerCDFQuantile_le (hbdd n ω) (le_of_lt hx)`). No file other than Quantiles.lean references it. `grep -c 'lowerCDFQuantile_le' inventory/ch10-inventory.md` = 0 (the inventory lists `lowerCDFQuantile`, `lowerCDFQuantile_bracket_of_local_right_lt`, `lowerCDFQuantile_bracket_of_stieltjesFunction`, etc., but not `lowerCDFQuantile_le`). AGENTS.md line 14: "Keep same-file proof scaffolding private." Line 84 of Quantiles.lean shows the sibling helper already marked `private theorem stieltjesFunction_exists_right_lt_of_lt`.
  - suggested fix: Add the `private` keyword: `private theorem lowerCDFQuantile_le`.
  - mechanical: true | confidence: high
- **[nit]** `lt_lowerCDFQuantile_of_exists_right_lt` (line 45) — Public `_of_...` bridge lemma `lt_lowerCDFQuantile_of_exists_right_lt` is only used as proof infrastructure inside its own file (once, in `lowerCDFQuantile_bracket_of_local_right_lt`), with zero external callers and no inventory reference.
  - rule: AGENTS.md core-principles-4 make-_of_-bridges-private
  - evidence: `rg -l 'lt_lowerCDFQuantile_of_exists_right_lt' --glob '*.lean' | grep -v Quantiles.lean` = empty. In-file `rg -n` shows only line 45 (def) and line 79 (`exact lt_lowerCDFQuantile_of_exists_right_lt (hmono n ω) (hne n ω) (hlocal n ω x hx)` inside `lowerCDFQuantile_bracket_of_local_right_lt`). Search over `*.md`/`*.json`/`*.txt` returns no inventory reference. AGENTS.md lines 87-88: "`_of_...` bridge lemmas are usually proof infrastructure. Make them private unless downstream files should cite them directly." The sibling helper `stieltjesFunction_exists_right_lt_of_lt` at line 84 is already `private`, matching the suggested fix.
  - suggested fix: Add the `private` keyword: `private theorem lt_lowerCDFQuantile_of_exists_right_lt`.
  - mechanical: true | confidence: high
- **[nit]** `bootstrapScalarCDF_exists_right_lt_of_lt` (line 605) — Public `_of_...` bridge lemma `bootstrapScalarCDF_exists_right_lt_of_lt` is only consumed in-file by `bootstrapScalarCDF_local_right_lt_of_aemeasurable`; it has zero external callers and is not part of the documented inventory crosswalk.
  - rule: AGENTS.md core-principles-4 make-_of_-bridges-private
  - evidence: `rg -l 'bootstrapScalarCDF_exists_right_lt_of_lt' --glob '*.lean' | grep -v Quantiles.lean` = empty (no external callers). All occurrences: Quantiles.lean:605 (definition, `theorem` with no `private`) and Quantiles.lean:637 (the only use, inside `bootstrapScalarCDF_local_right_lt_of_aemeasurable`). `rg 'bootstrapScalarCDF_exists_right_lt' inventory/ch10-inventory.md` = empty (not in crosswalk). AGENTS.md line 87-88: "`_of_...` bridge lemmas are usually proof infrastructure. Make them private unless downstream files should cite them directly." Current line 605 confirmed public: `theorem bootstrapScalarCDF_exists_right_lt_of_lt`.
  - suggested fix: Add the `private` keyword: `private theorem bootstrapScalarCDF_exists_right_lt_of_lt`.
  - mechanical: true | confidence: medium
- **[nit]** `bootstrapScalarCDFIndexed_exists_right_lt_of_lt` (line 1340) — Public `_of_...` bridge lemma `bootstrapScalarCDFIndexed_exists_right_lt_of_lt` (indexed counterpart of the same scaffolding) is used only in-file by `bootstrapScalarCDFIndexed_local_right_lt_of_aemeasurable`, has zero external callers, and is not in the inventory.
  - rule: AGENTS.md core-principles-4 make-_of_-bridges-private
  - evidence: `rg -l bootstrapScalarCDFIndexed_exists_right_lt_of_lt --glob '*.lean' | grep -v Quantiles.lean` returns empty (no external callers). In Quantiles.lean the name appears only at line 1340 (declaration `theorem ...`, public) and line 1376 (its sole use inside `bootstrapScalarCDFIndexed_local_right_lt_of_aemeasurable`). `rg -n ... inventory/ch10-inventory.md` returns exit 1 (not in inventory). AGENTS.md line 87-88: "`_of_...` bridge lemmas are usually proof infrastructure. Make them private unless downstream files should cite them directly."
  - suggested fix: Add the `private` keyword: `private theorem bootstrapScalarCDFIndexed_exists_right_lt_of_lt`.
  - mechanical: true | confidence: medium

## Draft fixes

6 mechanical finding(s) sent to 2 fixer worktree(s). Per-file outcomes:

```
The target file does not exist in this worktree. The worktree is checked out at commit `0227417` (a May-24 merge predating the Chapter 10 work), not at the `codex/chapter-10-complete` branch where `HansenEconometrics/Chapter10Bootstrap/HigherOrder.lean` actually lives. Switching branches or checking out the file would be a non-mechanical action outside the allowed-edit list and could clobber worktree state, so per the "when in doubt, do nothing" constraint I made no changes.

[{"status":"report_only","decl":"secondOrder_scaled_probability_transfer","reason":"target file not present in this worktree: HansenEconometrics/Chapter10Bootstrap/HigherOrder.lean does not exist at the checked-out commit (worktree HEAD = 0227417, a merge predating the Chapter 10 work). The file exists only on branch codex/chapter-10-complete. Applying the docstring edit would require switching branches or checking out the file, which is non-mechanical and outside the allowed-edit list. No edit applied; build not attempted.","build_error":null}]
```
```
The target file is absent and the working tree is clean. I cannot apply any edit. All five findings are downgraded to report-only.

[{"status":"report_only","decl":"Chapter10Bootstrap.Quantiles","reason":"target file missing: HansenEconometrics/Chapter10Bootstrap/Quantiles.lean does not exist in this worktree. The worktree (branch worktree-wf_52107a72-598-22) is checked out at commit 0227417, which predates all Chapter 10 Bootstrap work — there are zero Chapter10 files tracked or on disk. Cannot apply the module docstring edit. No build attempted; working tree clean.","build_error":null},{"status":"report_only","decl":"lowerCDFQuantile_le","reason":"target file missing: HansenEconometrics/Chapter10Bootstrap/Quantiles.lean does not exist in this worktree (checkout at commit 0227417, predates Chapter 10 work). Cannot add the private keyword. No build attempted.","build_error":null},{"status":"report_only","decl":"lt_lowerCDFQuantile_of_exists_right_lt","reason":"target file missing: HansenEconometrics/Chapter10Bootstrap/Quantiles.lean does not exist in this worktree (checkout at commit 0227417, predates Chapter 10 work). Cannot add the private keyword. No build attempted.","build_error":null},{"status":"report_only","decl":"bootstrapScalarCDF_exists_right_lt_of_lt","reason":"target file missing: HansenEconometrics/Chapter10Bootstrap/Quantiles.lean does not exist in this worktree (checkout at commit 0227417, predates Chapter 10 work). Cannot add the private keyword. No build attempted.","build_error":null},{"status":"report_only","decl":"bootstrapScalarCDFIndexed_exists_right_lt_of_lt","reason":"target file missing: HansenEconometrics/Chapter10Bootstrap/Quantiles.lean does not exist in this worktree (checkout at commit 0227417, predates Chapter 10 work). Cannot add the private keyword. No build attempted.","build_error":null}]
```
