# Lean Review Report

Reviewed 1 file(s); 3 confirmed finding(s).

## HansenEconometrics/Chapter9HypothesisTesting.lean

### hygiene

- **[nit]** `quadraticForm_tendstoInDistribution_of_vector_and_matrix` (line 331) — Public Slutsky/CMT bridge theorem used only once inside this file (in nonlinearHausmanStatOrZero_tendstoInDistribution_chiSquared_of_limitLaw) and absent from both the module docstring public-surface list and inventory/ch9-inventory.md; a helper whose removal would only break this file should usually be private.
  - rule: AGENTS.md keep-scaffolding-private
  - evidence: `rg -l 'quadraticForm_tendstoInDistribution_of_vector_and_matrix' --glob '*.lean'` returns only HansenEconometrics/Chapter9HypothesisTesting.lean. `rg -n` shows exactly two hits in that file: definition at line 331 (`theorem quadraticForm_tendstoInDistribution_of_vector_and_matrix`) and the sole call site at line 583 (`have hquad := quadraticForm_tendstoInDistribution_of_vector_and_matrix`). `rg 'quadraticForm_tendstoInDistribution_of_vector_and_matrix' inventory/ch9-inventory.md` exits 1 (no match). The module docstring (lines 4-40) lists the public surface and does not mention this decl. AGENTS.md line 87-88: "`_of_...` bridge lemmas are usually proof infrastructure. Make them private unless downstream files should cite them directly." and line 98-99: "A helper whose removal would only break the file where it is defined should usually be `private`."
  - suggested fix: Add the `private` keyword to `theorem quadraticForm_tendstoInDistribution_of_vector_and_matrix` (or add an inventory entry if it is meant to be a reusable public bridge).
  - mechanical: true | confidence: low

### proof-quality

- **[minor]** `hasLaw_gaussian_mahalanobis_noncentralChiSquared` (line 137) — The proof manually re-proves continuity of the dotProduct pairing via `continuous_finset_sum`/`simpa [dotProduct]`, redoing infrastructure that Mathlib's `Continuous.dotProduct` already provides (and which this same repo uses elsewhere).
  - rule: AGENTS.md Proof strategy policy (avoid redoing infrastructure available in Mathlib)
  - evidence: Mathlib signature (Mathlib/Topology/Instances/Matrix.lean:139-141): `protected theorem Continuous.dotProduct [Fintype n] [Mul R] [AddCommMonoid R] [ContinuousAdd R] [ContinuousMul R] {A : X → n → R} {B : X → n → R} (hA : Continuous A) (hB : Continuous B) : Continuous fun x => A x ⬝ᵥ B x`. Existing repo usages of Continuous.dotProduct: Chapter5LikelihoodRatioTest.lean:494, Chapter5NormalRegression.lean:427, Chapter8Asymptotics.lean:447, Chapter10Bootstrap/WLLN.lean:2133, Chapter10Bootstrap/Covariance.lean:750. Build check: replaced the hdot block with `Continuous.dotProduct continuous_fst continuous_snd` and ran `lake env lean HansenEconometrics/Chapter9HypothesisTesting.lean` → EXIT: 0 (no errors). File then restored to original (git diff clean).
  - suggested fix: Replace the `hdot` block with `have hdot : Continuous (fun p : (Fin q → ℝ) × (Fin q → ℝ) => p.1 ⬝ᵥ p.2) := Continuous.dotProduct continuous_fst continuous_snd`.
  - mechanical: false | confidence: high
- **[minor]** `quadraticForm_tendstoInDistribution_of_vector_and_matrix` (line 331) — The proof again manually re-proves continuity of the dotProduct pairing with `continuous_finset_sum`/`simpa [dotProduct]`, duplicating the same hand-rolled block from `hasLaw_gaussian_mahalanobis_noncentralChiSquared` instead of using Mathlib's `Continuous.dotProduct`.
  - rule: AGENTS.md Proof strategy policy (avoid redoing infrastructure available in Mathlib)
  - evidence: Lines 347-352 of HansenEconometrics/Chapter9HypothesisTesting.lean:
  have hdot : Continuous (fun p : (q → ℝ) × (q → ℝ) => p.1 ⬝ᵥ p.2) := by
    classical
    simpa [dotProduct] using
      (continuous_finset_sum Finset.univ (fun i _ =>
        (((continuous_apply i).comp continuous_fst).mul
          ((continuous_apply i).comp continuous_snd))))
This is identical (modulo q vs Fin q) to lines 149-154. `Continuous.dotProduct` is already used in the repo: Chapter5LikelihoodRatioTest.lean:494 and Chapter5NormalRegression.lean:427. I applied the suggested fix `have hdot ... := Continuous.dotProduct continuous_fst continuous_snd` and `lake env lean HansenEconometrics/Chapter9HypothesisTesting.lean` returned EXIT: 0 with no diagnostics, then reverted the edit (working tree clean except the pre-existing untracked review/reports/codex-quantiles-redundancy.json).
  - suggested fix: Replace the `hdot` block with `have hdot : Continuous (fun p : (q → ℝ) × (q → ℝ) => p.1 ⬝ᵥ p.2) := Continuous.dotProduct continuous_fst continuous_snd`.
  - mechanical: false | confidence: high

## Draft fixes

1 mechanical finding(s) sent to 1 fixer worktree(s). Per-file outcomes:

```
Confirmed: there is no Chapter9 file anywhere in this worktree. The worktree's branch (`worktree-wf_43b8ac40-8f0-9` based on commit `0227417`) does not contain `HansenEconometrics/Chapter9HypothesisTesting.lean` — the codebase only goes up to Chapter 8 on this base.

The target file does not exist, so the mechanical edit cannot be applied. Downgrading to report-only.

[{"status":"report_only","decl":"quadraticForm_tendstoInDistribution_of_vector_and_matrix","reason":"Target file HansenEconometrics/Chapter9HypothesisTesting.lean does not exist in this worktree. The worktree branch (worktree-wf_43b8ac40-8f0-9, based on commit 0227417) only contains chapters up to Chapter 8; no Chapter9 file exists anywhere in the worktree (verified via `ls HansenEconometrics/` and `find . -iname '*chapter9*'`, both showing no match). The edit could not be applied because there is no file to edit.","build_error":null}]
```
