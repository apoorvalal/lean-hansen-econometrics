# Lean Review Report

Reviewed 15 file(s); 62 confirmed finding(s).

## HansenEconometrics/Chapter10Bootstrap/Covariance.lean

### hygiene

- **[major]** `<module>` (line 1) — This 10,268-line module with ~190 public declarations has no module-level docstring; a reader cannot tell what the file covers or which declarations form the intended chapter-facing public surface.
  - rule: AGENTS.md PR-gate: give large modules a docstring explaining contents and public surface
  - evidence: `wc -l` = 10268 lines. `rg -F '/-!' Covariance.lean` => exit=1 (zero matches, no `/-! ... -/` block anywhere). File head: line 1 `import HansenEconometrics.Chapter10Bootstrap.Variance`, lines 3-5 `open ...`, line 7 `namespace HansenEconometrics`, lines 9-12 `variable ...`, line 14 `section BootstrapCovariance`, line 16 first `/--` (a per-decl docstring, not module-level). Public decl count via `rg -c '^(noncomputable )?(def|theorem|lemma|abbrev|structure|instance) '` = 128; `rg -c '^private '` = 4. AGENTS.md:18 `- Give large modules a docstring that explains their contents and public surface.`
  - suggested fix: Add a top-of-file `/-! ... -/` module docstring summarizing the chapter-10 bootstrap covariance content (moment-form vs cov-form covariance, the indexed variants, the trimmed-statistic API layer, and the chapter10_* consistency/CLT results that constitute the chapter-facing public surface).
  - mechanical: true | confidence: high
- **[nit]** `abs_dotProduct_le_l1_mul_norm` (line 577) — This generic dot-product bound is public but is used only once inside this file and nowhere else in the repo; unlike the documented `Hansen's trimmed bootstrap statistic` API family it is plain proof scaffolding with no chapter-facing role, so it should be private (though it is a generic inequality that could arguably be retained for reuse — hence low confidence).
  - rule: AGENTS.md Public-API-and-theorem-hygiene: keep same-file proof scaffolding private
  - evidence: `rg -l 'abs_dotProduct_le_l1_mul_norm' --glob '*.lean'` returns only HansenEconometrics/Chapter10Bootstrap/Covariance.lean. `rg -n` in that file: line 577 `theorem abs_dotProduct_le_l1_mul_norm` (definition, no `private` keyword), line 632 the sole call site inside abs_dotProduct_le_l1_mul_norm's only caller. Statement at 579: `|x ⬝ᵥ a| ≤ (∑ j : k, |a j|) * ‖x‖` — a generic linear-algebra inequality with no bootstrap-specific content.
  - suggested fix: Add the `private` keyword to `abs_dotProduct_le_l1_mul_norm` (or, if intended as a reusable utility, lift it to a shared utilities file and document the intent).
  - mechanical: true | confidence: low
- **[nit]** `measure_strict_norm_gt_le_inv_sq_mul_integral_norm_sq` (line 6951) — This generic Markov/Chebyshev tail bound is public but is used only internally (twice) in this file and by no other repo file; it is proof scaffolding for the trimming-tail argument rather than a chapter-facing result, so it should be private (low confidence because it is a generic reusable bound that could legitimately stay public).
  - rule: AGENTS.md Public-API-and-theorem-hygiene: keep same-file proof scaffolding private
  - evidence: `rg -l 'measure_strict_norm_gt_le_inv_sq_mul_integral_norm_sq' --glob '*.lean'` returns only `HansenEconometrics/Chapter10Bootstrap/Covariance.lean`. In-file occurrences: line 6951 (definition, `theorem` — public), 7021, 7060 (uses). The decl is declared as a public `theorem` (no `private` keyword) at line 6951. AGENTS.md line 14 "Keep same-file proof scaffolding private" and lines 98-99 "A helper whose removal would only break the file where it is defined should usually be `private`." Note: docstring (lines 6946-6950) frames it as the trimming-tail form for Hansen Thm 10.12, but the statement is a generic Markov bound `{P : Measure Ωs} {Z : Ωs → E}` with no bootstrap-specific types and zero external callers, so it functions as in-file scaffolding.
  - suggested fix: Add the `private` keyword to `measure_strict_norm_gt_le_inv_sq_mul_integral_norm_sq` (or lift it to a shared probability-utilities file if it is meant to be reused).
  - mechanical: true | confidence: low

## HansenEconometrics/Chapter10Bootstrap/DeltaMethod.lean

### redundancy

- **[major]** `chapter10_bootstrap_smooth_function_gaussian_of_compact_range_closeness` (line 2594) — This public theorem has a byte-identical statement to chapter10_bootstrap_delta_method_gaussian_of_compact_range_closeness (line 252) — same hypotheses and conclusion, with no distinguishing hlinearization hypothesis — and its proof is a pure pass-through, creating a second canonical public surface for the same result.
  - rule: AGENTS.md Public API — one canonical public surface (no double canon)
  - evidence: diff of `sed -n '253,279p'` vs `sed -n '2595,2621p'` returned no differences (printed "=== SIGNATURES IDENTICAL ==="). Proof body lines 2623-2626: `chapter10_bootstrap_delta_method_gaussian_of_compact_range_closeness (μ := μ) (Pstar := Pstar) (Tstar := Tstar) (thetaStar := thetaStar) (V := V) G hV hT hK hPstar hTstar hthetaStar hlinearized_mem hthetaStar_mem hclose`. Usage check: `rg` for chapter10_bootstrap_smooth_function_gaussian_of_compact_range_closeness shows only the definition at line 2594 — ZERO callers (in-file or cross-file), so removing it is safe. The original chapter10_bootstrap_delta_method_gaussian_of_compact_range_closeness has multiple callers (lines 339, 409, 971, 2623). Both conclusions are TendstoInBootstrapWeakDistribution μ Pstar thetaStar (multivariateGaussian (0) (G * V * Gᵀ)) (fun z => z), identical at lines 278-280 and 2620-2622.
  - suggested fix: Remove this theorem; callers should use chapter10_bootstrap_delta_method_gaussian_of_compact_range_closeness directly (or keep one and make the other a deprecated alias if a Theorem-10.7-facing name is desired).
  - mechanical: true | confidence: medium
- **[major]** `chapter10_bootstrap_smooth_function_gaussian_of_compact_tail_closeness` (line 2631) — This public theorem has a byte-identical statement to chapter10_bootstrap_delta_method_gaussian_of_compact_tail_closeness (line 477) with no distinguishing hlinearization hypothesis, and its proof is a pure pass-through to that theorem, creating a duplicate public surface.
  - rule: AGENTS.md Public API — one canonical public surface (no double canon)
  - evidence: diff of `sed -n '478,509p'` (the line-477 theorem signature) vs `sed -n '2632,2663p'` (the line-2631 theorem signature) prints nothing and the script reports "SIGNATURES IDENTICAL". Proof body lines 2665-2668: `chapter10_bootstrap_delta_method_gaussian_of_compact_tail_closeness (μ := μ) (Pstar := Pstar) (Tstar := Tstar) (thetaStar := thetaStar) (V := V) G hV hT hPstar hTstar hthetaStar hTail hclose` — a verbatim forward of the same hypotheses, no extra argument. `rg -n 'chapter10_bootstrap_smooth_function_gaussian_of_compact_tail_closeness' --glob '*.lean'` returns only the single declaration line (DeltaMethod.lean:2631) — zero in-file and zero cross-file callers. `rg 'private theorem ...'` for both names exits 1, confirming both are public.
  - suggested fix: Remove this theorem and use chapter10_bootstrap_delta_method_gaussian_of_compact_tail_closeness directly.
  - mechanical: true | confidence: medium
- **[major]** `chapter10_bootstrap_smooth_function_gaussian_event_probability_of_compact_tail_closeness` (line 2672) — This public theorem duplicates chapter10_bootstrap_delta_method_gaussian_event_probability_of_compact_tail_closeness (line 529): identical hypotheses/conclusion with no hlinearization argument, proved by a direct pass-through call.
  - rule: AGENTS.md Public API — one canonical public surface (no double canon)
  - evidence: `diff` of statement bodies (DeltaMethod.lean lines 531–569 vs 2674–2712) shows the only difference is the proof-opener `:= by` vs `:=`; statements are otherwise identical. Proof at lines 2713–2716: `chapter10_bootstrap_delta_method_gaussian_event_probability_of_compact_tail_closeness (μ := μ) (Pstar := Pstar) (Tstar := Tstar) (thetaStar := thetaStar) (V := V) G hV hT hPstar hTstar hthetaStar hTail hclose hA hfrontier`. `rg` for the smooth_function name returns only its own definition (line 2673) — zero external callers. `rg` for the delta_method name returns its definition (530) plus uses at 812 and 2713 (the latter being this wrapper). Both theorems lie inside `section BootstrapDeltaMethod` (lines 14–4468) sharing `variable {μ : Measure Ω}`.
  - suggested fix: Remove this theorem and use chapter10_bootstrap_delta_method_gaussian_event_probability_of_compact_tail_closeness directly.
  - mechanical: true | confidence: medium
- **[major]** `chapter10_bootstrap_smooth_function_gaussian_distribution_of_compact_tail_closeness` (line 2720) — This public theorem duplicates chapter10_bootstrap_delta_method_gaussian_distribution_of_compact_tail_closeness (line 597): same statement with no hlinearization argument, proved by a pure pass-through.
  - rule: AGENTS.md Public API — one canonical public surface (no double canon)
  - evidence: `diff` of lines 599-641 (decl 597) vs 2722-2764 (decl 2720) shows the only difference is the proof delimiter `:= by` vs `:=`; the full signatures (binders {d r}, Pstar/Tstar/thetaStar/V, G, hV, hT, hPstar, hTstar, hthetaStar, hTail, hclose, hfrontier) and the conclusion `TendstoInBootstrapDistribution μ Pstar (fun n ω ωs => (thetaStar n ω ωs : r → ℝ)) (multivariateGaussian 0 (G*V*Gᵀ)) (fun z => (z : r → ℝ))` are identical. The line-2720 body (lines 2765-2768) is `chapter10_bootstrap_delta_method_gaussian_distribution_of_compact_tail_closeness (μ:=μ)(Pstar:=Pstar)(Tstar:=Tstar)(thetaStar:=thetaStar)(V:=V) G hV hT hPstar hTstar hthetaStar hTail hclose hfrontier` — a pure pass-through. `rg -n 'chapter10_bootstrap_smooth_function_gaussian_distribution_of_compact_tail_closeness'` returns only the definition site (line 2721), confirming zero in-file and zero cross-file callers.
  - suggested fix: Remove this theorem and use chapter10_bootstrap_delta_method_gaussian_distribution_of_compact_tail_closeness directly.
  - mechanical: true | confidence: medium
- **[major]** `chapter10_bootstrap_smooth_function_gaussian_event_probability_of_compact_range_closeness` (line 3222) — This public theorem duplicates chapter10_bootstrap_delta_method_gaussian_event_probability_of_compact_range_closeness (line 300): identical statement with no hlinearization argument, proved by a direct pass-through.
  - rule: AGENTS.md Public API — one canonical public surface (no double canon)
  - evidence: diff of the two signatures (HansenEconometrics/Chapter10Bootstrap/DeltaMethod.lean lines 301-334 vs 3223-3256) shows only two differences: the theorem name and the proof delimiter (`:= by` vs `:=`); all hypotheses and the conclusion are identical. Usage check: `rg -n 'chapter10_bootstrap_smooth_function_gaussian_event_probability_of_compact_range_closeness' --glob '*.lean'` returns exactly one hit — the declaration itself at line 3223 — so there are zero callers in-file and zero cross-file. Removal of the duplicate and direct use of the line-300 theorem is safe.
  - suggested fix: Remove this theorem and use chapter10_bootstrap_delta_method_gaussian_event_probability_of_compact_range_closeness directly.
  - mechanical: true | confidence: medium
- **[major]** `chapter10_bootstrap_smooth_function_gaussian_distribution_of_compact_range_closeness` (line 3264) — This public theorem duplicates chapter10_bootstrap_delta_method_gaussian_distribution_of_compact_range_closeness (line 362): same statement with no hlinearization argument, proved by a pure pass-through.
  - rule: AGENTS.md Public API — one canonical public surface (no double canon)
  - evidence: `diff` of the two signatures (lines 364-401 vs 3266-3303) shows the ONLY difference is line 401 `... := by` (real proof) vs line 3303 `... :=` (pass-through). Line 3304-3307 proof: `chapter10_bootstrap_delta_method_gaussian_distribution_of_compact_range_closeness (μ := μ) (Pstar := Pstar) (Tstar := Tstar) (thetaStar := thetaStar) (V := V) G hV hT hK hPstar hTstar hthetaStar hlinearized_mem hthetaStar_mem hclose hfrontier`. Usage check: `rg -n 'chapter10_bootstrap_smooth_function_gaussian_distribution_of_compact_range_closeness' --glob '*.lean'` returns only the definition site (DeltaMethod.lean:3265) — zero external or in-file callers.
  - suggested fix: Remove this theorem and use chapter10_bootstrap_delta_method_gaussian_distribution_of_compact_range_closeness directly.
  - mechanical: true | confidence: medium

### hygiene

- **[major]** `module` (line 1) — This 4470-line module exports ~90 public theorems but has no module-level docstring explaining its contents or public surface; the only docstrings are attached to individual theorems, so a reader cannot tell from the top of the file what the module covers or what its chapter-facing API is.
  - rule: AGENTS.md PR-gate give-large-modules-a-docstring
  - evidence: `wc -l` = 4470. `rg '/-!'` over the whole file returns no matches (exit 1) — confirming no module docstring exists, not just at the top. Public theorem count: `rg '^\s*theorem'` = 92 (the finding's "~90"); the lower count of 26 from `^theorem ` is an artifact of the common style where the keyword `theorem` sits on its own line with the (long) name on the next line, e.g. lines 300-301 `theorem` / `chapter10_bootstrap_delta_method_gaussian_event_probability_of_compact_range_closeness`. `rg '^\s*(private|protected) '`, `rg '^\s*(def|lemma|noncomputable) '` both return zero (exit 1), so every declaration is a public theorem. Lines 1-14: import → open/open scoped → `namespace HansenEconometrics` → variable block → `section BootstrapDeltaMethod`, with no `/-! -/`. The first docstring at line 16 (`/-- Hansen Theorem 10.6, linearized bootstrap Delta-method bridge...`) documents the theorem `chapter10_bootstrap_delta_method_linear`, not the module.
  - suggested fix: Add a `/-! # Chapter 10 bootstrap Delta method ... -/` module docstring after the imports (before or just inside `namespace HansenEconometrics`) summarizing the module's scope (Hansen Theorem 10.6 bootstrap Delta-method bridges: linear/matrix-linear/Gaussian variants, indexed variants, smooth-function linearization variants, compact-range/tail closeness and remainder-bound families) and naming the canonical public entry points.
  - mechanical: true | confidence: high

## HansenEconometrics/Chapter10Bootstrap/Distribution.lean

### hygiene

- **[nit]** `TendstoInBootstrapDistribution.congr` (line 112) — TendstoInBootstrapDistribution.congr is a one-line convenience wrapper composing congr_bootstrap and congr_limit, but it has no in-file or cross-file callers (verified via rg dot-notation search across all .lean files), so it could be private; uncertainty: it mirrors the symmetric public TendstoInBootstrapWeakDistribution.congr API whose siblings (tendsto_cdf, congr_bootstrap) ARE used externally, so this may be an intentionally-parallel public surface.
  - rule: AGENTS.md keep-same-file-scaffolding-private
  - evidence: `rg -n 'TendstoInBootstrapDistribution\.congr\b' --glob='*.lean'` -> only `HansenEconometrics/Chapter10Bootstrap/Distribution.lean:112:theorem TendstoInBootstrapDistribution.congr`. Proof body (lines 119-120) is `(hZ.congr_bootstrap hstar).congr_limit hlim`. The component lemmas ARE used externally: `congr_bootstrap` in DeltaMethod.lean (2248, 2354, 3369, 3472), `congr_limit` in Percentile.lean:2354 and Studentization.lean:330,357 — but the composed `.congr` itself has no callers. Symmetric sibling check: `rg -n 'TendstoInBootstrapWeakDistribution\.congr\b'` -> only its definition (WeakDistribution.lean:225), i.e. also uncited externally. AGENTS.md:14 "Keep same-file proof scaffolding private"; AGENTS.md:98-99 "A helper whose removal would only break the file where it is defined should usually be private."
  - suggested fix: Mark `private theorem TendstoInBootstrapDistribution.congr` unless a downstream file is expected to cite it; if kept public for API symmetry with the indexed/weak variants, leave as-is.
  - mechanical: true | confidence: low
- **[nit]** `TendstoInBootstrapDistributionIndexed.congr` (line 216) — TendstoInBootstrapDistributionIndexed.congr is a one-line wrapper composing congr_bootstrap and congr_limit with no in-file or cross-file callers (verified via rg), so it could be private; uncertainty: it is the indexed counterpart of a symmetric public congruence API (the sibling congr_bootstrap/congr_limit ARE used externally in DeltaMethod.lean and Percentile.lean), so this may be intentionally public for parallel-API consistency.
  - rule: AGENTS.md keep-same-file-scaffolding-private
  - evidence: `rg 'TendstoInBootstrapDistributionIndexed\.congr\b'` -> only Distribution.lean:216 (the definition). External sibling usage verified: `.congr_bootstrap`/`.congr_limit` appear in DeltaMethod.lean:2248,2354,3369,3472, Studentization.lean:330,357, Percentile.lean:2354 — but the combined `.congr` never. Suspicious bare `.congr` candidates in other files are on unrelated receivers: Covariance.lean:2019 and WeakDistribution.lean:3690 `hdot.congr` are on `AEMeasurable hdot` (AEMeasurable.congr); most others are `TendstoInMeasure.congr`. Proof body lines 223-224: `(hZ.congr_bootstrap hstar).congr_limit hlim`.
  - suggested fix: Mark `private theorem TendstoInBootstrapDistributionIndexed.congr` unless a downstream file is expected to cite it; if kept public for symmetry with the externally-used congr_bootstrap/congr_limit, leave as-is.
  - mechanical: true | confidence: low

## HansenEconometrics/Chapter10Bootstrap/Empirical.lean

### hygiene

- **[major]** `<module>` (line 1) — This 6657-line module exposes ~120 public declarations but has no module-level docstring (`/-! ... -/`) explaining its contents and public surface.
  - rule: AGENTS.md PR-gate: give large modules a docstring
  - evidence: `wc -l HansenEconometrics/Chapter10Bootstrap/Empirical.lean` => 6657. `rg -c '/-!' Empirical.lean` => no matches (exit 1), so there is no `/-! ... -/` module docstring anywhere in the file. The file opens with `import` lines 1-9, `open`/`open scoped` lines 11-13, and `namespace HansenEconometrics` at line 15 — no doc block precedes the namespace. Public declaration count via `rg -c '^(theorem|lemma|def|noncomputable def|abbrev|structure|instance) '` => 114 (plus 39 `private`), far exceeding any "large module" threshold. AGENTS.md line 18: "Give large modules a docstring that explains their contents and public surface."
  - suggested fix: Add a `/-! # Empirical distribution and bootstrap moments ... -/` module docstring after the imports summarizing the file's contents and its public API surface (empirical mean / bootstrap resample identities, cumulant moment formulas, characteristic-function CLT bridge).
  - mechanical: true | confidence: high
- **[nit]** `centeredEmpiricalCharFunFinSucc_remainder_norm_le_integral_split` (line 5262) — This public `_remainder_norm_le_` bridge lemma is proof infrastructure used only inside this file and should be `private`.
  - rule: AGENTS.md "Public API and theorem hygiene": keep same-file proof scaffolding private; _of_ bridges are proof infrastructure
  - evidence: `rg -l 'centeredEmpiricalCharFunFinSucc_remainder_norm_le_integral_split' --glob='*.lean'` returns only `HansenEconometrics/Chapter10Bootstrap/Empirical.lean`. In-file occurrences: line 5262 `theorem centeredEmpiricalCharFunFinSucc_remainder_norm_le_integral_split` (definition, no `private` keyword) and line 5547 `centeredEmpiricalCharFunFinSucc_remainder_norm_le_integral_split (Y := Y) (n := n) ... hsmall` inside a `have hbound :=` term (sole internal use). Inventory grep: `centeredEmpiricalCharFunFinSucc_remainder_norm_le_integral_split` does not appear in inventory/ch10-inventory.md; the inventoried bridge (lines 620, 816) cites `centeredEmpiricalCharFunFinSucc_remainder_isLittleO_of_variance_tendsto_tail`, not this remainder-split helper.
  - suggested fix: Add the `private` keyword to `theorem centeredEmpiricalCharFunFinSucc_remainder_norm_le_integral_split`.
  - mechanical: true | confidence: high
- **[nit]** `centeredEmpiricalCharFunFinSucc_remainder_isLittleO_of_scaled_norm_tendsto_zero` (line 5655) — This public `_isLittleO_of_scaled_norm_tendsto_zero` bridge lemma is only used internally and should be `private`.
  - rule: AGENTS.md §4: _of_ bridge lemmas are proof infrastructure; make them private unless downstream files cite them
  - evidence: `rg -l 'centeredEmpiricalCharFunFinSucc_remainder_isLittleO_of_scaled_norm_tendsto_zero' --glob '*.lean'` returns only `HansenEconometrics/Chapter10Bootstrap/Empirical.lean`. In-file `rg -n` shows exactly two occurrences: line 5655 (the `theorem` definition, no `private` keyword) and line 5700 (used inside `_of_variance_bound_tail`'s proof). `rg` over inventory/ch10-inventory.md returns no match. AGENTS.md lines 87-88: "`_of_...` bridge lemmas are usually proof infrastructure. Make them private unless downstream files should cite them directly."
  - suggested fix: Add the `private` keyword to `theorem centeredEmpiricalCharFunFinSucc_remainder_isLittleO_of_scaled_norm_tendsto_zero`.
  - mechanical: true | confidence: high
- **[nit]** `centeredEmpiricalCharFunFinSucc_remainder_isLittleO_of_variance_bound_tail` (line 5683) — This public `_isLittleO_of_variance_bound_tail` bridge lemma is only used internally (consumed by the inventoried `_of_variance_tendsto_tail` wrapper) and should be `private`.
  - rule: AGENTS.md §4: _of_ bridge lemmas are proof infrastructure; make them private unless downstream files cite them
  - evidence: `rg -l 'centeredEmpiricalCharFunFinSucc_remainder_isLittleO_of_variance_bound_tail' --glob '*.lean'` -> only `HansenEconometrics/Chapter10Bootstrap/Empirical.lean`. In-file `rg -n` -> 5684 (definition), 5806 (docstring mention inside `_of_variance_tendsto_tail`'s doc), 5841 (sole call site, inside the body of public `centeredEmpiricalCharFunFinSucc_remainder_isLittleO_of_variance_tendsto_tail` at line 5807). AGENTS.md §4 line 87-88: "`_of_...` bridge lemmas are usually proof infrastructure. Make them private unless downstream files should cite them directly." The fixer can safely prepend `private` to `theorem centeredEmpiricalCharFunFinSucc_remainder_isLittleO_of_variance_bound_tail` at line 5683 since there are no external callers.
  - suggested fix: Add the `private` keyword to `theorem centeredEmpiricalCharFunFinSucc_remainder_isLittleO_of_variance_bound_tail`.
  - mechanical: true | confidence: medium

## HansenEconometrics/Chapter10Bootstrap/FiniteReplication.lean

### hygiene

- **[major]** `FiniteReplication` (line 1) — This is an exceptionally large module (17,893 lines, 328 declarations of which 327 are public, forming the chapter-facing finite-replication variance/covariance estimator API for Hansen Theorem 10.11) with no module-level `/-! ... -/` docstring explaining its contents and public surface.
  - rule: AGENTS.md PR-gate: give large modules a docstring
  - evidence: `wc -l HansenEconometrics/Chapter10Bootstrap/FiniteReplication.lean` = 17893. `rg -n '/-!' FiniteReplication.lean` returns no matches (exit 1) — no module-header doc-comment. File head goes import (line 1) -> open scoped (lines 3-5) -> `namespace HansenEconometrics` (line 7) -> variable (lines 9-12) -> `section FiniteReplicationVariance` (line 14); first `/--` is a per-declaration docstring on `finiteReplicationMeanReal` (line 16). `rg -cP '^\s*(noncomputable\s+)?(private\s+|protected\s+)?(theorem|lemma|def|abbrev|structure|instance|class)\b'` = 328 declarations; `rg -nP '^\s*private\b'` = exactly 1 (line 92 `tendstoInMeasure_of_integral_norm_sq_le_inv`), so 327 public. AGENTS.md line 18: "Give large modules a docstring that explains their contents and public surface." Sole importer `Chapter10Bootstrap.lean` line 1 only `import`s the file and never cites a specific declaration. Sibling convention confirmed: `AsymptoticUtils/DeltaMethod.lean` and `AsymptoticUtils/StochasticOrder.lean` both contain `/-!` module docstrings.
  - suggested fix: Add a `/-! # Chapter 10 finite-replication bootstrap variance/covariance ... -/` module docstring after the imports/opens (before `namespace`), summarizing the estimator definitions (`finiteReplicationMeanReal`/`...SecondMomentReal`/`...CovarianceCenteredReal`/`...Mat`), the L²-error / TendstoInMeasure constructor infrastructure (`tendstoInMeasure_zero_of_integral_sq_error_le_inv`, etc.), the centered/moment bridge lemmas (`_eq_momentReal`/`_eq_momentMat`), and the large `chapter10_*` / `chapter10_indexed_*` Theorem-10.11 convergence theorem families that form the chapter-facing public API.
  - mechanical: true | confidence: high

### proof-quality

- **[minor]** `chapter10_finiteReplicationVariance_tendsto_of_moments` (line 401) — This 62-line `by` proof re-derives the moment-form convergence block (hmean_sq / hsecond0 / hdiff0 / hdiff / hfactor / hprod / congr) from scratch, which is exactly the diagonal X=Y special case of the more general `chapter10_finiteReplicationCovarianceReal_tendsto_of_moments` (L5998); the variance result could delegate to the covariance theorem instead of duplicating the proof idea.
  - rule: AGENTS.md §2 reuse-repo-theorems-before-adding (rubric: copy-pasted proof block instead of citing the earlier theorem)
  - evidence: Definitions (L22-47): finiteReplicationSecondMomentReal Z = (B)⁻¹*∑(Z B b ω)^2; finiteReplicationCrossMomentReal X Y = (B)⁻¹*∑ X*Y; finiteReplicationVarianceMomentReal Z = correction*(SecondMoment - mean^2); finiteReplicationCovarianceMomentReal X Y = correction*(CrossMoment - meanX*meanY). Variance proof L410-463: hmean_sq (mul_limits_real hmean hmean), hsecond0 (L416), hmean_sq0 (L417), hdiff0 (L418-429 via sub_zero_real + congr + ring), hdiff (L430-437 of_sub_limit_zero_real), hfactor (L438-443 tendstoInMeasure_const_real), hprod (L444-451 simpa pow_two mul_limits_real), final TendstoInMeasure.congr (L452-463). Covariance proof L6009-6051: hmean_prod (mul_limits_real hmeanX hmeanY), hcross0, hmean_prod0, hdiff0 (sub_zero_real + congr + ring), hdiff (of_sub_limit_zero_real), hfactor (identical), hprod (mul_limits_real), simpa. Precedent L371-379: `simpa [finiteReplicationVarianceCenteredReal, finiteReplicationVarianceMomentReal, finiteReplicationCovarianceMomentReal, finiteReplicationCrossMomentReal, finiteReplicationSecondMomentReal, pow_two] using finiteReplicationCovarianceCenteredReal_eq_momentReal (X := Z) (Y := Z) hB ω`. The covariance theorem (L5998) is independent of the variance theorem (it calls only mul_limits_real/sub helpers, not chapter10_finiteReplicationVariance_tendsto_of_moments), so the suggested reordering is feasible. AGENTS.md §2 (L40-42): "Prefer composing results already proved in this repo over duplicating algebra in a later chapter. If a later theorem is really just a corollary or a notation bridge, write it as such."
  - suggested fix: Reorder so chapter10_finiteReplicationCovarianceReal_tendsto_of_moments precedes the variance bridge, then replace this proof body with a delegation: apply the covariance theorem with X := Z, Y := Z and bridge `finiteReplicationVarianceMomentReal`/`SecondMomentReal`/`^2` to `finiteReplicationCovarianceMomentReal`/`CrossMomentReal Z Z`/`mul` via `simpa [..., pow_two] using ...`, mirroring the existing finiteReplicationVarianceCenteredReal_eq_momentReal pattern.
  - mechanical: false | confidence: medium

## HansenEconometrics/Chapter10Bootstrap/HigherOrder.lean

### hygiene

- **[minor]** `HigherOrder` (line 1) — This 896-line module with a 27-theorem public surface (the entire Hansen 10.15/10.17 higher-order refinement API) has no module-level docstring explaining its contents and public surface.
  - rule: AGENTS.md PR-gate give-large-modules-a-docstring
  - evidence: `wc -l` = 895 lines. `rg '/-!'` over the file returns nothing (grep exit 1) — no module-level docstring. First `/--` is at line 17: "/-- Generic second-order probability transfer." which is the docstring of `theorem secondOrder_scaled_probability_transfer` (line 24), not a module docstring. Lines 1-16 (non-empty) contain only imports (1-2), opens (4-6), `namespace HansenEconometrics` (8), variables (10-13), and `section HigherOrderRefinements` (15). `rg -c '(^|\s)(theorem|lemma) '` = 19 declarations; `rg -c '^private '` = 0 (no matches), so all 19 are public. AGENTS.md:18 "Give large modules a docstring that explains their contents and public surface."; AGENTS.md:312 lists "add docstring" among mechanical PR-gate fixes.
  - suggested fix: Add a module-level /-! ... -/ docstring after the imports/opens summarizing the Theorem 10.15 (percentile-t interval) and Theorem 10.17 (two-sided test) second-order Edgeworth transfer API and naming the public entry points.
  - mechanical: true | confidence: medium

### proof-quality

- **[nit]** `chapter10_abs_test_secondOrder_rejection_expansion_polynomial` (line 699) — The proof is a single direct application wrapped in a redundant `by exact ...` tactic block, inconsistent with the file's sibling polynomial wrappers (e.g. lines 380 and 797) which write the identical-shaped delegation in pure term mode; the `by exact` adds no proof content.
  - rule: AGENTS.md Proof strategy policy (direct chapter-native proofs / avoid needless tactic wrapping)
  - evidence: Lines 715-724: `... atTop (𝓝 0) := by\n  exact\n    chapter10_abs_test_secondOrder_rejection_expansion (μ := μ) ... h (edgeworthP1Polynomial_neg a0 a2 c) (edgeworthP2Polynomial_neg b1 b3 b5 c) hdensity halpha`. Sibling at line 797 (chapter10_abs_test_secondOrder_rejection_expansion_polynomial_one_sub_alpha) writes the same delegation in term mode at lines 813-820: `... atTop (𝓝 0) :=\n  chapter10_abs_test_secondOrder_rejection_expansion_one_sub_alpha (μ := μ) ... h ... hdensity hcoverage` with no `by exact`. Sibling at line 380 (chapter10_percentileT_secondOrder_interval_expansion_polynomial_one_sub_alpha) likewise uses term mode `:=` at lines 395-402. Other siblings at lines 728 (line 750) and 761 (line 785) also use term mode `:=`. The line-699 wrapper is the lone outlier using `by exact` around a single term application.
  - suggested fix: Drop the `by exact` wrapper and write the body as a term-mode proof `:= chapter10_abs_test_secondOrder_rejection_expansion (μ := μ) ... h (edgeworthP1Polynomial_neg a0 a2 c) (edgeworthP2Polynomial_neg b1 b3 b5 c) hdensity halpha`, matching the sibling wrappers.
  - mechanical: false | confidence: medium

## HansenEconometrics/Chapter10Bootstrap/Percentile.lean

### hygiene

- **[major]** `<module>` (line 1) — This 4328-line module exposes a very large public surface (~70 public declarations spanning percentile-coverage, bias-correction, and BCa interval APIs) but has no module-level `/-!` docstring explaining its contents and public surface.
  - rule: AGENTS.md PR-gate docstrings-for-large-modules
  - evidence: `wc -l HansenEconometrics/Chapter10Bootstrap/Percentile.lean` = 4328. `rg -n '/-!' Percentile.lean` returns nothing (exit code 1 — zero matches). Lines 1-15 show the file opens with `import HansenEconometrics.Chapter10Bootstrap.Quantiles`/`...Regression`, then `open` statements, then `namespace HansenEconometrics` (line 8) and `section PercentileIntervals` (line 15) with no `/-! ... -/` block. `rg -c '^(noncomputable def|def|theorem|lemma|abbrev|structure|instance) '` = 47 public top-level decls vs only 7 `private`. `rg -c 'chapter10_'` = 83 occurrences; 18 bca/biasCorrected def/theorem/lemma decls. The single `section PercentileIntervals` spans lines 15-4326 (rg shows `15:section PercentileIntervals` and `4326:end PercentileIntervals`).
  - suggested fix: Add a top-of-file `/-! # Chapter 10: Percentile and BCa confidence intervals ... -/` module docstring summarizing the three main public groups (percentile-interval coverage, bias-corrected (BC) intervals, BCa intervals) and the canonical chapter-facing theorems.
  - mechanical: true | confidence: high
- **[nit]** `frontier_percentileCoverageSet_subset` (line 190) — `frontier_percentileCoverageSet_subset` is a pure-topology scaffolding lemma about the internal `percentileCoverageSet`, used only inside this file and not externally, yet it is public while its sibling scaffolding lemma `isClosed_percentileCoverageSet` (line 125) is `private` — the visibility is inconsistent and it should be `private`.
  - rule: AGENTS.md public-API keep-same-file-scaffolding-private
  - evidence: `rg -l frontier_percentileCoverageSet_subset --glob '*.lean'` returns only `HansenEconometrics/Chapter10Bootstrap/Percentile.lean`. `rg -n` shows exactly two occurrences: the declaration at line 190 (`theorem frontier_percentileCoverageSet_subset :`) and one in-file use at line 237 (`frontier_percentileCoverageSet_subset hboundary_zero`). No file other than Percentile.lean references the name, so the hygiene refutation condition (out-of-file callers) does not trigger. Line 125 confirms the sibling helper is private: `private theorem isClosed_percentileCoverageSet : IsClosed percentileCoverageSet := by`. Other in-file scaffolding helpers (e.g. `aemeasurable_percentileCoverageLimitVector` at line 160) are also `private`, reinforcing that the public visibility here is an inconsistency.
  - suggested fix: Add the `private` keyword: `private theorem frontier_percentileCoverageSet_subset`.
  - mechanical: true | confidence: medium
- **[nit]** `percentileCoverage_frontier_null_of_boundary_null` (line 211) — `percentileCoverage_frontier_null_of_boundary_null` is internal measure-theoretic scaffolding (null-frontier from boundary null mass) used only within this file and nowhere externally, yet it is public while the parallel scaffolding lemma `aemeasurable_percentileCoverageLimitVector` (line 160) is `private`; it should be `private`.
  - rule: AGENTS.md public-API keep-same-file-scaffolding-private
  - evidence: `rg -l 'percentileCoverage_frontier_null_of_boundary_null' --glob '*.lean'` returns only HansenEconometrics/Chapter10Bootstrap/Percentile.lean. `rg -n` shows: line 207 (the `theorem` definition; the finding's cited line 211 is actually the `hright` hypothesis line of the same decl) and in-file uses at lines 261 and 417 — no other files. The sibling helper is `private theorem aemeasurable_percentileCoverageLimitVector` at line 156 (declared `private`). AGENTS.md lines 14, 87, 99 establish the keep-scaffolding-private / `_of_...`-bridge-private rule. Minor: the finding's line numbers (160/211/265/421) are each ~4 lines off from actual (156/207/261/417), but identify the correct declarations.
  - suggested fix: Add the `private` keyword: `private theorem percentileCoverage_frontier_null_of_boundary_null`.
  - mechanical: true | confidence: medium
- **[nit]** `chapter10_percentileCI_coverage_indexed_finSucc_olsBetaOrZero_gapEnvelope_bounds_sampleCLT_brackets_gaussian_of_robustFeasibleHCMomentConditions` (line 3456) — This public theorem identifier is 144 characters long and is essentially impossible to cite in prose; AGENTS.md asks to use namespaces and shorter local names. Uncertainty: the name follows a deliberate systematic `_of_`/feature-suffix convention shared by a family of theorems, so this may be an intentional naming scheme rather than a hygiene defect.
  - rule: AGENTS.md PR-gate keep-theorem-names-citeable
  - evidence: `printf '%s' '<name>' | wc -c` => 144. Line 3452-3453: `theorem` / `    chapter10_percentileCI_coverage_indexed_finSucc_olsBetaOrZero_gapEnvelope_bounds_sampleCLT_brackets_gaussian_of_robustFeasibleHCMomentConditions` — a public theorem (not `private`). AGENTS.md:16 `- Keep theorem names citeable; use namespaces and local names to avoid very long identifiers.` Namespace scan of the file shows only `namespace HansenEconometrics` (line 8) and `section PercentileIntervals` (line 15 / end 4322); there is NO `namespace PercentileIntervals`, so the suggested fix (introduce a nested namespace to drop the shared prefix) is applicable. `rg -c '^    chapter10_percentileCI'` => 39 sibling theorems sharing the long `chapter10_percentileCI_coverage_` prefix.
  - suggested fix: Introduce a nested namespace (e.g. `namespace PercentileCI.SampleCLT`) so the local name can drop the long shared prefix/suffix qualifiers and become citeable, or provide a short documented abbreviation in the docstring.
  - mechanical: false | confidence: low

### proof-quality

- **[nit]** `percentileCoverageLimitVector_mem_set_iff` (line 149) — The proof rewrites the goal into a syntactically identical `P ↔ P` via `change` and then closes with `rfl`; since membership of `percentileCoverageLimitVector ξ qLower qUpper ω` in `percentileCoverageSet` unfolds definitionally to `qLower ≤ -ξ ω ∧ -ξ ω ≤ qUpper`, the iff is reflexivity and the multi-line `change` block is likely unnecessary scaffolding. Uncertainty: the `Fin 3` if-decisions in `percentileCoverageLimitVector` may require the explicit `change` to force reduction, so `rfl`/`Iff.rfl` alone may not close it.
  - rule: AGENTS.md Proof strategy policy (golfable single-tactic proof)
  - evidence: Edited Percentile.lean line 149-156, replacing the `change ... rfl` body of `percentileCoverageLimitVector_mem_set_iff` with `:= Iff.rfl`. `lake build HansenEconometrics.Chapter10Bootstrap.Percentile` after `touch` of the file: "Build completed successfully (3131 jobs)." with no errors or warnings. percentileCoverageSet (line 122-123) = {z | z 1 ≤ -z 0 ∧ -z 0 ≤ z 2}; percentileCoverageLimitVector (lines 38-43) maps 0↦ξ ω, 1↦qLower, 2↦qUpper via if-then-else on Fin 3, so the LHS membership is defeq to the RHS and Iff.rfl closes it. Edit was reverted via git checkout after verification.
  - suggested fix: Attempt replacing the `change ...; rfl` body with a single `Iff.rfl` term proof (or just `rfl`); if the Fin 3 if-decisions block reduction, keep the current form.
  - mechanical: false | confidence: low

## HansenEconometrics/Chapter10Bootstrap/PercentileT.lean

### hygiene

- **[major]** `<module>` (line 1) — This 7929-line module with ~80 public declarations has no module-level docstring explaining its contents or public surface; AGENTS.md requires large modules to carry such a docstring.
  - rule: AGENTS.md PR-gate: give large modules a docstring
  - evidence: `wc -l` = 7929 lines. `rg -c '/-!' PercentileT.lean` returns no matches (zero module-docstring blocks). File head (lines 1-7): line 1 `import HansenEconometrics.Chapter10Bootstrap.Percentile`, lines 3-5 `open ...`, line 7 `namespace HansenEconometrics` — no `/-! ... -/` overview. `rg -c '^(theorem|lemma|def|...) '` = 24; loose `rg -c '\b(theorem|lemma|def) '` = 30. The 89 `/--` hits are per-declaration docstrings, not the required module-level `/-!` block. AGENTS.md line 18: "- Give large modules a docstring that explains their contents and public surface."
  - suggested fix: Add a top-of-file `/-! # Percentile-t confidence intervals ... -/` module docstring summarizing the public surface (coverage limit theorems, OLS/HC specializations) and the internal scaffolding layering.
  - mechanical: true | confidence: high
- **[nit]** `percentileTCoverageVector_mem_set_iff` (line 145) — This public lemma is pure in-file glue (it `change`s the membership goal and closes with `(...).symm`) used only once inside this file and never cited cross-file or in the inventory crosswalk, so it should be `private` like its neighbouring scaffolding helpers.
  - rule: AGENTS.md hygiene: keep same-file proof scaffolding private
  - evidence: `rg -l 'percentileTCoverageVector_mem_set_iff' --glob '*.lean'` returns only HansenEconometrics/Chapter10Bootstrap/PercentileT.lean (no external callers, so the hygiene refutation test passes). Occurrences: line 145 (def) and line 331 (`exact percentileTCoverageVector_mem_set_iff (Ω := Ω) (hse n ω)`) — a single in-file use. `rg` over `*.md` finds it absent from the inventory crosswalk. Body (lines 151-155) is a `change`+`exact (...).symm` glue lemma. Adjacent helpers `isClosed_percentileTCoverageSet` (115) and `aemeasurable_percentileTCoverageLimitVector` (171) are already `private`. AGENTS.md:14 "Keep same-file proof scaffolding private." Fix is safe: zero external callers.
  - suggested fix: Prepend `private` to the `theorem percentileTCoverageVector_mem_set_iff` declaration.
  - mechanical: true | confidence: high
- **[nit]** `percentileTCIEvent_iff_tstat_between` (line 124) — This public lemma is used only once inside this file and is not cited cross-file or in the inventory crosswalk; it appears to be internal scaffolding that should be `private`, though its clean reusable docstring leaves some chance it is intended as chapter-facing API.
  - rule: AGENTS.md hygiene: keep same-file proof scaffolding private
  - evidence: `rg -l 'percentileTCIEvent_iff_tstat_between' --glob '*.lean'` -> only HansenEconometrics/Chapter10Bootstrap/PercentileT.lean (no external file). `rg -c` in that file -> 2 (declaration at line 124 + single use at line 155 inside percentileTCoverageVector_mem_set_iff: `exact (percentileTCIEvent_iff_tstat_between hse).symm`). `rg -c 'percentileTCIEvent_iff_tstat_between' inventory/ch10-inventory.md` -> exit 1 (absent). By contrast `rg -c 'percentileTCoverageLimit_measure_set_eq|frontier_percentileTCoverageSet_subset' inventory/ch10-inventory.md` -> 3 (siblings cited). The 496KB inventory file exists. The declaration is `theorem` (public) at line 124, while adjacent helpers like isClosed_percentileTCoverageSet (line 115) and aemeasurable_percentileTCoverageLimitVector (line 171) are already `private`.
  - suggested fix: Prepend `private` to the declaration unless it is intended as a citeable chapter-facing fact, in which case add an inventory crosswalk entry instead.
  - mechanical: true | confidence: low

### proof-quality

- **[nit]** `percentileTCoverageLimitVector_mem_set_iff` (line 159) — The proof uses `change` to rewrite the goal into a syntactically identical proposition and closes with `rfl`; since both sides of the iff are definitionally and printed-identically equal, the whole proof is just `Iff.rfl`, so the `change` block is redundant scaffolding. Uncertainty: the rubric excludes minor tactic-level choices and readability `change`/`have`, and the `change` arguably documents the membership unfolding, so rule applicability is genuinely borderline.
  - rule: AGENTS.md Proof strategy policy (golfable one-step proof)
  - evidence: Source lines 159-167: theorem body is `change (qLower ≤ ξ ω ∧ ξ ω ≤ qUpper) ↔ qLower ≤ ξ ω ∧ ξ ω ≤ qUpper` followed by `rfl`. percentileTCoverageSet := {z | z 1 ≤ z 0 ∧ z 0 ≤ z 2} (line 112-113) and percentileTCoverageLimitVector ξ qLower qUpper ω = fun i => if i=0 then ξ ω else if i=1 then qLower else qUpper (line 39-44), so membership unfolds definitionally to qLower ≤ ξ ω ∧ ξ ω ≤ qUpper. I replaced the body with `:= Iff.rfl` and ran `lake build HansenEconometrics.Chapter10Bootstrap.PercentileT` → "Build completed successfully (3132 jobs)." (then reverted the edit). Rubric review/rubric.md line 208 confirms this counts; lines 216-217 exclusions reference `exact h`/`have`, neither of which covers a 4-line `change ... rfl` block. No external-call/usage concern since this is a proof-quality (not removal) finding.
  - suggested fix: Replace the `change ...; rfl` body with `Iff.rfl` (the membership and the conjunction are definitionally equal).
  - mechanical: false | confidence: low

## HansenEconometrics/Chapter10Bootstrap/Quantiles.lean

### hygiene

- **[major]** `Quantiles` (line 1) — This 2114-line module exposes a very large public surface (~80 public declarations spanning scalar/bootstrap/indexed CDFs and quantile-convergence wrappers) but has no module-level docstring explaining its contents and public surface.
  - rule: AGENTS.md PR-gate give-large-modules-a-docstring
  - evidence: File header (lines 1-16): line 1 `import Mathlib.Probability.CDF`, lines 5-7 `open`, line 9 `namespace HansenEconometrics`, line 16 `section QuantileConvergence` — no `/-!` module doc precedes any of these. `rg -n '/-!' Quantiles.lean` returns zero matches (no module-section docstring anywhere in the file). The first `/--` (line 18) is attached to the `CDFQuantileBracket` structure (line 24), not a module overview. `wc -l` = 2113 lines; `rg -c` for declaration keywords = 84 declarations. AGENTS.md line 18 states the rule verbatim.
  - suggested fix: Add a module docstring (`/-! ... -/`) at the top summarizing the file's contents and the canonical public entry points (e.g. `bootstrapScalarLowerQuantile*_tendsto_*` quantile-convergence theorems and the `scalarCDF`/`bootstrapScalarCDF` definitions).
  - mechanical: true | confidence: high
- **[nit]** `lt_lowerCDFQuantile_of_exists_right_lt` (line 45) — This `_of_...` bridge lemma is pure proof infrastructure used only inside this file (to build `lowerCDFQuantile_bracket_of_local_right_lt`) but is public.
  - rule: AGENTS.md public-API keep-same-file-scaffolding-private
  - evidence: `rg -l 'lt_lowerCDFQuantile_of_exists_right_lt' --glob='*.lean'` returns only HansenEconometrics/Chapter10Bootstrap/Quantiles.lean. `rg -n` shows exactly 2 occurrences: definition at line 45 (`theorem lt_lowerCDFQuantile_of_exists_right_lt`) and one in-file use at line 79 inside `lowerCDFQuantile_bracket_of_local_right_lt`. The file already uses the `private` keyword for an analogous plumbing helper at line 84 (`private theorem stieltjesFunction_exists_right_lt_of_lt`). AGENTS.md line 87-88: "`_of_...` bridge lemmas are usually proof infrastructure. Make them private unless downstream files should cite them directly." AGENTS.md line 98-99: "A helper whose removal would only break the file where it is defined should usually be `private`."
  - suggested fix: Add the `private` keyword (matching the existing `private theorem stieltjesFunction_exists_right_lt_of_lt` convention at line 84).
  - mechanical: true | confidence: medium
- **[nit]** `lowerCDFQuantile_le` (line 35) — This generalized-inverse helper lemma is used only inside this file but is public.
  - rule: AGENTS.md public-API keep-same-file-scaffolding-private
  - evidence: `rg -l 'lowerCDFQuantile_le' --glob='*.lean'` -> only HansenEconometrics/Chapter10Bootstrap/Quantiles.lean. `rg -n '\blowerCDFQuantile_le\b' --glob='*.lean'` -> exactly 2 hits, both in Quantiles.lean: line 35 (definition `theorem lowerCDFQuantile_le`) and line 82 (`exact lowerCDFQuantile_le (hbdd n ω) (le_of_lt hx)` inside `lowerCDFQuantile_bracket_of_local_right_lt`). Word-boundary regex rules out spurious prefix matches against other `lowerCDFQuantile_*` names. AGENTS.md:14 "Keep same-file proof scaffolding private."; AGENTS.md:98-99 "A helper whose removal would only break the file where it is defined should usually be `private`." The adjacent helper `stieltjesFunction_exists_right_lt_of_lt` (line 84) is already marked `private`, confirming this is the file's convention for such plumbing.
  - suggested fix: Add the `private` keyword.
  - mechanical: true | confidence: medium
- **[nit]** `lowerCDFQuantile_bracket_of_local_right_lt` (line 67) — This `_of_...` bracket-constructor bridge lemma is used only inside this file but is public.
  - rule: AGENTS.md public-API keep-same-file-scaffolding-private
  - evidence: `rg -l 'lowerCDFQuantile_bracket_of_local_right_lt' --glob='*.lean'` returns only `HansenEconometrics/Chapter10Bootstrap/Quantiles.lean`. `rg -n` shows exactly 4 occurrences, all in that file: def at line 67 (declared as `theorem ...`, not `private`), uses at lines 116, 252, 274. Compare sibling helper at line 84: `private theorem stieltjesFunction_exists_right_lt_of_lt` — confirming the file's private-scaffolding convention. Zero external callers, so the `private` edit is safe.
  - suggested fix: Add the `private` keyword.
  - mechanical: true | confidence: low
- **[nit]** `lowerCDFQuantile_bracket_of_stieltjesFunction` (line 109) — This `_of_...` bracket-constructor bridge lemma is used only inside this file but is public.
  - rule: AGENTS.md public-API keep-same-file-scaffolding-private
  - evidence: `rg -l 'lowerCDFQuantile_bracket_of_stieltjesFunction' --glob='*.lean'` returns only HansenEconometrics/Chapter10Bootstrap/Quantiles.lean. `rg -n` shows 3 occurrences all in that file: definition at line 109 (`theorem lowerCDFQuantile_bracket_of_stieltjesFunction`), and in-file uses at line 294 and line 314 (`(hbracket := lowerCDFQuantile_bracket_of_stieltjesFunction hne hbdd)`). No `private` keyword precedes the theorem at line 109. AGENTS.md line 87-88: "`_of_...` bridge lemmas are usually proof infrastructure. Make them private unless downstream files should cite them directly." — no downstream file cites it.
  - suggested fix: Add the `private` keyword.
  - mechanical: true | confidence: low
- **[nit]** `bootstrapScalarCDF_eq_cdf_map` (line 585) — This bridge lemma identifying `bootstrapScalarCDF` with Mathlib's `cdf` of the push-forward is used only inside this file but is public.
  - rule: AGENTS.md public-API keep-same-file-scaffolding-private
  - evidence: `rg -l 'bootstrapScalarCDF_eq_cdf_map' --glob='*.lean'` → only `HansenEconometrics/Chapter10Bootstrap/Quantiles.lean` (no external files). In-file occurrences = 4: definition at line 585 (`theorem bootstrapScalarCDF_eq_cdf_map`), internal uses at lines 619, 662, 694. AGENTS.md line 98-99: "A helper whose removal would only break the file where it is defined should usually be `private`." AGENTS.md line 14: "Keep same-file proof scaffolding private." The declaration is currently `theorem` (public, no `private` keyword) at line 585. Suggested fix (add `private`) is valid; the three in-file callers at 619/662/694 are in the same file so they continue to resolve after the change.
  - suggested fix: Add the `private` keyword.
  - mechanical: true | confidence: low
- **[nit]** `bootstrapScalarCDF_exists_right_lt_of_lt` (line 605) — This `_of_lt` right-continuity bracketing bridge lemma is used only inside this file but is public.
  - rule: AGENTS.md public-API keep-same-file-scaffolding-private
  - evidence: `rg -l 'bootstrapScalarCDF_exists_right_lt_of_lt' --glob='*.lean'` returns only HansenEconometrics/Chapter10Bootstrap/Quantiles.lean. `rg -n` shows exactly 2 occurrences in that file: the definition (`theorem` at line 605, no `private` keyword) and one use at line 637 inside `bootstrapScalarCDF_local_right_lt_of_aemeasurable`. No external callers. AGENTS.md:14 "Keep same-file proof scaffolding private." and AGENTS.md:87-88 "`_of_...` bridge lemmas are usually proof infrastructure. Make them private unless downstream files should cite them directly."
  - suggested fix: Add the `private` keyword.
  - mechanical: true | confidence: low

## HansenEconometrics/Chapter10Bootstrap/Regression.lean

### redundancy

- **[major]** `heteroAsymCov_posSemidef_of_scoreCLTConditions` (line 17) — heteroAsymCov_posSemidef_of_scoreCLTConditions re-proves, with a byte-for-byte identical proof body, the heteroskedastic sandwich PosSemidef result already established as heteroAsymCov_posSemidef in the earlier chapter file Chapter8Asymptotics.lean (line 3476), rather than reusing it.
  - rule: AGENTS.md §2 reuse-repo-theorems-before-adding-new-ones
  - evidence: Ch10 Regression.lean lines 23-31 proof body is identical to Ch8Asymptotics.lean lines 3481-3489: `let A := (popGram μ X)⁻¹; have hΩ := scoreCovMat_posSemidef ... h.toSampleCLTAssumption72; have hA : Aᵀ = A := by simpa [A] using (popGram_inv_isSymm ... h.toSampleMomentAssumption71.int_outer).eq; have hpsd ... Matrix.PosSemidef.mul_mul_conjTranspose_same hΩ A; simpa [heteroAsymCov, A, hA] using hpsd`. The underlying building blocks (heteroAsymCov, scoreCovMat_posSemidef, popGram_inv_isSymm) live in Chapter7, so Ch8's version is itself canonical, not a Mathlib delegation. Import-closure BFS over Regression.lean's transitive imports (30 modules) confirms Chapter8Asymptotics is NOT imported — the suggested fix is correctly a cross-file change. USAGE CHECK (for fixer): the Ch10 decl HAS external callers outside Regression.lean — HansenEconometrics/Chapter10Bootstrap/FiniteReplication.lean (lines 13289, 13367, 13454, 13538) and Chapter10Bootstrap/Percentile.lean (line 2821), in addition to ~20 in-file callers. Therefore a naive delete-the-duplicate is unsafe; the fix must keep the name as a thin alias (`:= heteroAsymCov_posSemidef h`) after adding the import, rather than removing the declaration.
  - suggested fix: Delete the duplicated proof and delegate to the Chapter 8 theorem: `theorem heteroAsymCov_posSemidef_of_scoreCLTConditions ... := heteroAsymCov_posSemidef h`. This requires adding `import HansenEconometrics.Chapter8Asymptotics` to Regression.lean's transitive closure (Chapter8Asymptotics is not currently imported), so it is a cross-file change. Alternatively make the Chapter 10 declaration a thin alias once the import is added.
  - mechanical: false | confidence: high

### hygiene

- **[major]** `<module>` (line 1) — This 8300-line module with a very large public surface (160+ public declarations) has no module-level docstring explaining its contents and public API; the file begins directly with imports, `namespace`, and a theorem.
  - rule: AGENTS.md PR-gate: give large modules a docstring
  - evidence: `wc -l` = 8300 lines. `rg -- '/-!' Regression.lean` returns no matches (exit code 1) — no module docstring anywhere in the file. `rg -c '^(theorem|lemma|def|abbrev|structure|class|instance|noncomputable)'` = 184 top-level declarations; `rg -c 'private '` = 3, so 180+ are public (exceeds the claimed "160+"). Lines 1-2 are `import HansenEconometrics.Chapter10Bootstrap.{Covariance,Studentization}`, line 8 `namespace HansenEconometrics`, line 17 the first theorem `heteroAsymCov_posSemidef_of_scoreCLTConditions` — confirming the file begins directly with imports/namespace/theorem with no docstring block.
  - suggested fix: Add a `/-! ... -/` module docstring near the top (after imports) summarizing the chapter-10 bootstrap-regression contents and naming the chapter-facing public API (the `chapter10_bootstrap_regression_*` / `chapter10_indexed_bootstrap_regression_*` families).
  - mechanical: true | confidence: high
- **[nit]** `regressionBootstrap_sampleCrossMoment_errors_finSucc_eq_resampleMean` (line 112) — This `_eq_` bridge lemma is public but is pure proof infrastructure used only inside this file (it rewrites the resampled cross moment into a resample mean to feed `..._eq_sqrt_smul_sampleCrossMoment_sub`), with zero external callers.
  - rule: AGENTS.md PR-gate: keep same-file proof scaffolding private
  - evidence: `rg -l 'regressionBootstrap_sampleCrossMoment_errors_finSucc_eq_resampleMean' --glob '*.lean'` returns only `HansenEconometrics/Chapter10Bootstrap/Regression.lean`. `rg -n` confirms exactly two occurrences: line 112 (`theorem regressionBootstrap_sampleCrossMoment_errors_finSucc_eq_resampleMean`) and line 155 (`rw [regressionBootstrap_sampleCrossMoment_errors_finSucc_eq_resampleMean, ...]` inside `regressionBootstrapScoreFinSucc_eq_sqrt_smul_sampleCrossMoment_sub`). The declaration is introduced with the bare `theorem` keyword (no `private` modifier), confirming it is public. Zero external callers; marking it `private` is safe.
  - suggested fix: Mark the declaration `private` (it is only referenced at line 155 within this file).
  - mechanical: true | confidence: medium
- **[nit]** `sampleCrossMoment_stackRegressors_stackErrors_finSucc_eq_empiricalMean` (line 128) — This `_eq_` bridge lemma is public but is pure proof infrastructure used only inside this file (rewrites the original-sample cross moment into an empirical mean for the score-equality lemma), with zero external callers.
  - rule: AGENTS.md PR-gate: keep same-file proof scaffolding private
  - evidence: `rg -l 'sampleCrossMoment_stackRegressors_stackErrors_finSucc_eq_empiricalMean' --glob '*.lean'` returns only HansenEconometrics/Chapter10Bootstrap/Regression.lean. `rg -n` shows exactly two hits in that file: line 128 (theorem definition; not marked private) and line 156 (the rewrite use inside regressionBootstrapScoreFinSucc_eq_sqrt_smul_sampleCrossMoment_sub). No other .lean file references it. The lemma rewrites the original-sample cross moment into an empirical mean (lines 132-139), pure proof infrastructure. AGENTS.md line 14: "Keep same-file proof scaffolding private"; lines 98-99: "A helper whose removal would only break the file where it is defined should usually be private."
  - suggested fix: Mark the declaration `private`; its only consumer is `regressionBootstrapScoreFinSucc_eq_sqrt_smul_sampleCrossMoment_sub` at line 156.
  - mechanical: true | confidence: medium
- **[nit]** `regressionBootstrapScoreFinSucc_eq_sqrt_smul_sampleCrossMoment_sub` (line 144) — This `_eq_` bridge lemma is public but is proof scaffolding used only in-file (unfolds the bootstrap score into a sqrt-smul of a cross-moment difference), with zero external callers.
  - rule: AGENTS.md PR-gate: keep same-file proof scaffolding private
  - evidence: `rg -l 'regressionBootstrapScoreFinSucc_eq_sqrt_smul_sampleCrossMoment_sub' --glob='*.lean'` => only HansenEconometrics/Chapter10Bootstrap/Regression.lean. `rg -n` => line 144 (theorem definition, no `private` keyword), line 368 (internal `rw` use). AGENTS.md line 14 "Keep same-file proof scaffolding private", lines 87-88 "`_of_...` bridge lemmas ... Make them private unless downstream files should cite them directly", lines 98-99 "A helper whose removal would only break the file where it is defined should usually be `private`." Other private theorems do exist in this file (lines 1968, 2220, 5432), so the pattern is established.
  - suggested fix: Mark the declaration `private` since it is only consumed by later proofs within this file.
  - mechanical: true | confidence: medium
- **[nit]** `regressionBootstrapLinearRestrictionStatisticFinSucc_eq_theta_apply` (line 226) — This `_eq_` bridge lemma identifying the scalar restriction statistic with the Unit coordinate of the theta statistic is public but used only as in-file proof infrastructure, with zero external callers.
  - rule: AGENTS.md PR-gate: keep same-file proof scaffolding private
  - evidence: `rg -l 'regressionBootstrapLinearRestrictionStatisticFinSucc_eq_theta_apply' --glob '*.lean'` returns exactly one file: HansenEconometrics/Chapter10Bootstrap/Regression.lean. In-file occurrences: line 226 (definition, declared `theorem`, not `private`), and uses at lines 254 (in `..._abs_le_beta_norm`), 1998, and 5453 — all internal proofs. No external file references the declaration, so marking it `private` is safe.
  - suggested fix: Mark the declaration `private`; it is only referenced by in-file proofs (e.g. `..._abs_le_beta_norm` at line 254).
  - mechanical: true | confidence: medium
- **[nit]** `regressionBootstrapLinearRestrictionStatisticFinSucc_abs_le_beta_norm` (line 245) — This bounding bridge lemma is public but used only as in-file proof infrastructure (it is consumed by `..._eventually_abs_bound_of_beta_bound` to supply the scalar numerator bound), with zero external callers.
  - rule: AGENTS.md PR-gate: keep same-file proof scaffolding private
  - evidence: Source line 245: `theorem regressionBootstrapLinearRestrictionStatisticFinSucc_abs_le_beta_norm` (no `private` modifier). `rg -l 'regressionBootstrapLinearRestrictionStatisticFinSucc_abs_le_beta_norm' --glob '*.lean'` → only `HansenEconometrics/Chapter10Bootstrap/Regression.lean`. `rg -n` shows exactly 2 occurrences: definition at line 245 and single use at line 278 inside `regressionBootstrapLinearRestrictionStatisticFinSucc_eventually_abs_bound_of_beta_bound`. AGENTS.md:14 "Keep same-file proof scaffolding private."; AGENTS.md:87 "`_of_...` bridge lemmas are usually proof infrastructure. Make them private unless downstream files should cite them directly."; AGENTS.md:98-99 helper whose removal would only break its own file should usually be `private`.
  - suggested fix: Mark the declaration `private`; its only consumer is `regressionBootstrapLinearRestrictionStatisticFinSucc_eventually_abs_bound_of_beta_bound` within this file.
  - mechanical: true | confidence: medium

## HansenEconometrics/Chapter10Bootstrap/Studentization.lean

### hygiene

- **[minor]** `module` (line 1) — This 2181-line module exposes 46 public theorems (the full chapter-facing bootstrap-studentization API) yet has no module-level docstring explaining its contents and public surface, contrary to AGENTS.md's rule to give large modules such a docstring.
  - rule: AGENTS.md PR-gate large-module-docstring
  - evidence: `wc -l` = 2180. `rg '^/-!'` returns nothing; `rg '/-!'` anywhere in the file returns nothing (only `/--` per-decl docstrings starting at line 108). File begins directly with `import HansenEconometrics.Chapter7Asymptotics.Inference` (line 1), then `open`/`namespace`/`section` (lines 4-15), with no module docstring. Declaration counts: `rg -c '^(theorem|lemma) '` = 36 top-level; any-indent = 40; `rg -c 'private (theorem|lemma)'` = 4. AGENTS.md line 18: "Give large modules a docstring that explains their contents and public surface."
  - suggested fix: Add a `/-! # Bootstrap studentization ... -/` module docstring after the imports/open block summarizing the public surface: the Slutsky product constructor, the compact-tail constructors, and the Theorem 10.18/10.16 studentized-ratio weak/Definition-10.2/absolute bridges (fixed and indexed).
  - mechanical: true | confidence: medium

## HansenEconometrics/Chapter10Bootstrap/Tests.lean

### hygiene

- **[major]** `Tests` (line 1) — This 5913-line module with roughly 90 public declarations has no module-level docstring; line 1 is an import and there is no `/-! ... -/` or `/-- ... -/` block before `namespace HansenEconometrics` describing the file's contents or public surface.
  - rule: AGENTS.md PR gate: Give large modules a docstring that explains their contents and public surface
  - evidence: wc -l = 5913. Top of file (lines 1-9): `import HansenEconometrics.Chapter10Bootstrap.Quantiles` / `import HansenEconometrics.Chapter10Bootstrap.Regression` / blank / `open MeasureTheory ProbabilityTheory Filter` / `open scoped ENNReal Topology MeasureTheory ProbabilityTheory Matrix` / `open scoped Matrix.Norms.Elementwise Function` / blank / `namespace HansenEconometrics` / `variable ...` — no docstring or `/-! -/` before the namespace. Declaration count: `rg -c '^\s*(noncomputable )?(def|theorem|lemma|abbrev|structure|instance|class)\b'` = 94 total; `rg -c '^private '` = 2 private, leaving ~92 public, consistent with the finding's "roughly 90 public declarations." First `/--` docstring is at line 17 on `bootstrapAbsTestReject`, inside the namespace.
  - suggested fix: Add a module docstring (a `/-! # Chapter 10 bootstrap two-sided absolute tests ... -/` section comment) at the top of the file summarizing the rejection-region setup, the Theorem 10.16 rejection-probability bridges, and the regression-t-statistic specializations that form the public surface.
  - mechanical: true | confidence: high
- **[minor]** `bootstrapAbsTestVector_mem_rejectionSet_iff` (line 85) — bootstrapAbsTestVector_mem_rejectionSet_iff is public but has zero references anywhere in the repository (not used in-file, not used cross-file, and not cited in the chapter inventory), unlike its companion bootstrapAbsTestLimitVector_mem_rejectionSet_iff which is both used and crosswalked.
  - rule: AGENTS.md Public API and theorem hygiene: a helper whose removal would only break the file where it is defined should usually be private
  - evidence: `rg -n 'bootstrapAbsTestVector_mem_rejectionSet_iff' -g '*.lean'` -> only "HansenEconometrics/Chapter10Bootstrap/Tests.lean:85:theorem bootstrapAbsTestVector_mem_rejectionSet_iff" (1 total). `rg -n ... inventory/` -> no match. By contrast, the Limit companion: `rg -c 'bootstrapAbsTestLimitVector_mem_rejectionSet_iff' Tests.lean` -> 3, and it is crosswalked at inventory/ch10-inventory.md:957. The declaration at Tests.lean:85 is currently `theorem` (public); neighboring scaffolding lemmas (isOpen_bootstrapAbsRejectionSet:81, aemeasurable_bootstrapAbsTestLimitVector:103) are already `private`.
  - suggested fix: Mark the declaration `private` (`private theorem bootstrapAbsTestVector_mem_rejectionSet_iff`); since it has no callers at all, consider removing it if it is not needed for documentation.
  - mechanical: true | confidence: high
- **[nit]** `cdf_eq_zero_of_ae_nonneg` (line 528) — cdf_eq_zero_of_ae_nonneg is a public proof-infrastructure helper used only inside Tests.lean and not cited in the chapter inventory, so it should be private.
  - rule: AGENTS.md Public API and theorem hygiene: keep same-file proof scaffolding private
  - evidence: `rg -l 'cdf_eq_zero_of_ae_nonneg' -g '*.lean'` returns only HansenEconometrics/Chapter10Bootstrap/Tests.lean (no external callers). In-file count is 2 (definition at line 528, single use at line 560 inside cdf_brackets_of_ae_nonneg_strict). `rg -c 'cdf_eq_zero_of_ae_nonneg' inventory/` returns no matches, so it is not chapter-facing API. AGENTS.md line 14 ("Keep same-file proof scaffolding private") and lines 98-99 ("A helper whose removal would only break the file where it is defined should usually be private") establish the rule. The declaration at line 528 is currently `theorem` (not `private theorem`).
  - suggested fix: Add the `private` keyword: `private theorem cdf_eq_zero_of_ae_nonneg`.
  - mechanical: true | confidence: medium
- **[nit]** `cdf_brackets_of_ae_nonneg_strict` (line 543) — cdf_brackets_of_ae_nonneg_strict is a public proof-infrastructure helper used only inside Tests.lean and not cited in the chapter inventory, so it should be private.
  - rule: AGENTS.md Public API and theorem hygiene: keep same-file proof scaffolding private
  - evidence: `rg -l 'cdf_brackets_of_ae_nonneg_strict' -g '*.lean'` returns only HansenEconometrics/Chapter10Bootstrap/Tests.lean; `rg -c` in that file returns 4 (definition at line 543 plus uses). `rg -c 'cdf_brackets_of_ae_nonneg_strict' inventory/` returns nothing (exit 1), so it is not in the chapter inventory. The sole in-file caller is at line 585 inside cdf_brackets_of_hasLaw_ae_nonneg_strict. The decl is currently declared `theorem` (line 543) with no `private` modifier. AGENTS.md lines 87 ("`_of_...` bridge lemmas are usually proof infrastructure. Make them private unless downstream files should cite them directly") and 98-99 ("A helper whose removal would only break the file where it is defined should usually be `private`") directly govern this case. The fix (add `private`) is safe because there are zero external callers.
  - suggested fix: Add the `private` keyword: `private theorem cdf_brackets_of_ae_nonneg_strict`.
  - mechanical: true | confidence: medium
- **[nit]** `cdf_brackets_of_hasLaw_ae_nonneg_strict` (line 568) — cdf_brackets_of_hasLaw_ae_nonneg_strict is a public proof-infrastructure helper used only inside Tests.lean and not cited in the chapter inventory, so it should be private.
  - rule: AGENTS.md Public API and theorem hygiene: keep same-file proof scaffolding private
  - evidence: `rg -l 'cdf_brackets_of_hasLaw_ae_nonneg_strict' -g '*.lean'` returns only HansenEconometrics/Chapter10Bootstrap/Tests.lean (no out-of-file callers, satisfying the hygiene burden in verifier.md step 3). In-file it occurs 4 times: definition at line 568 (currently `theorem`, not `private theorem`) plus 3 uses at lines 1022, 1479, 1744. `rg -n ... inventory/` returns nothing, so it is absent from ch10-inventory.md (not chapter-facing API). AGENTS.md line 87-88: "`_of_...` bridge lemmas are usually proof infrastructure. Make them private unless downstream files should cite them directly." AGENTS.md line 98-99: "A helper whose removal would only break the file where it is defined should usually be `private`." The file already follows a private convention for some helpers (2 private decls present).
  - suggested fix: Add the `private` keyword: `private theorem cdf_brackets_of_hasLaw_ae_nonneg_strict`.
  - mechanical: true | confidence: medium
- **[nit]** `chapter10_indexed_abs_test_rejectionProb_of_regression_tstat_finSucc_olsBetaOrZero_gapEnvelope_scalarTail_of_robustFeasibleHCMomentConditions` (line 3073) — This public theorem name is 141 characters long (its strict companion at line 3299 is 148), which is too long to be reasonably cited in chapter prose; the file relies on very long compound `chapter10_..._of_...` identifiers instead of shorter namespaced local names.
  - rule: AGENTS.md PR gate: keep theorem names citeable; use namespaces and local names to avoid very long identifiers
  - evidence: `printf '%s' 'chapter10_indexed_abs_test_rejectionProb_of_regression_tstat_finSucc_olsBetaOrZero_gapEnvelope_scalarTail_of_robustFeasibleHCMomentConditions' | wc -c` → 141. Strict sibling at line 3299: `..._rejectionProb_strict_of_..._robustFeasibleHCMomentConditions` → 148. Source at lines 3073-3074 shows `theorem` (no `private`). Only namespace in file: `rg '^namespace ' Tests.lean` → line 8 `namespace HansenEconometrics` (no narrow sub-namespace). AGENTS.md:16: "Keep theorem names citeable; use namespaces and local names to avoid very long identifiers." Top name lengths: 148, 148, 144, 141, 141, 137, 112, 112, ... confirming systematic use of very long compound identifiers.
  - suggested fix: Shorten via a dedicated sub-namespace (e.g. `namespace RobustFeasibleHC` with a local name like `indexed_rejectionProb_finSucc_scalarTail`) so the fully qualified path stays citeable; note this is a public rename touching any downstream reference.
  - mechanical: false | confidence: low

## HansenEconometrics/Chapter10Bootstrap/Variance.lean

### redundancy

- **[minor]** `integral_tail_sq_matrixContinuousLinearMap_coord_le_norm_fourth` (line 5340) — This lemma re-derives from scratch the pointwise squared-tail domination argument (y² 1{|y|≥R} ≤ R⁻² y⁴, the by_cases hx block at lines 5410-5462) that the file's own earlier lemma integral_tail_sq_le_inv_sq_mul_integral_fourth (line 1587) already encapsulates, rather than composing it.
  - rule: AGENTS.md §2 reuse-repo-theorems / §4 bridge-lemmas
  - evidence: Earlier lemma 1587 proof (1604-1631): by_cases hx : R ≤ |Y x| → hR_sq_le: R^2 ≤ (Y x)^2 (1605-1606, via pow_le_pow_left₀ + sq_abs); hpoint: (Y x)^2 ≤ R⁻²·(Y x)^4 via field_simp/calc (1616-1621); Set.indicator_of_mem (1623); else branch indicator=0. Target lemma 5340 proof (5410-5462) reproduces this verbatim modulo renaming: hR_sq_le: R^2 ≤ y^2 (5411-5412, identical pow_le_pow_left₀ + sq_abs); hy_sq_le: y^2 ≤ (R^2)⁻¹·y^4 via field_simp/calc (5423-5428); Set.indicator_of_mem hxmem (5442); else branch indicator_of_notMem (5461). The only new content is hy4_le: y^4 ≤ C^4·‖Tx‖^4 (5400-5409) plus a closing integral_const_mul scaling. Existing composition exemplar at 1679-1683: `integral_tail_sq_le_inv_sq_mul_integral_fourth (P:=P) (Y:=fun i => ‖c • Y i‖) hε hfourth_int`. Usage check: `rg -l 'integral_tail_sq_matrixContinuousLinearMap_coord_le_norm_fourth' --glob '*.lean'` matches ONLY Variance.lean (5 occurrences: 1 def at 5340 + callers at 5529, 5605, 7682, 7791 — all intra-file). The earlier lemma is also intra-file only. Zero external callers, so the proposed refactor is safe.
  - suggested fix: Apply integral_tail_sq_le_inv_sq_mul_integral_fourth to Y := (coord of matrixContinuousLinearMap G (T ·)) to obtain `≤ R⁻²·∫(coord)⁴`, then bound `∫(coord)⁴ ≤ C⁴·∫‖T‖⁴` via integral_mono using hy4_le; this removes the duplicated by_cases tail argument.
  - mechanical: false | confidence: medium

### hygiene

- **[major]** `lindeberg_norm_sq_tail_normalized_uniformOn_finSucc_tendsto_zero_of_identDistrib_memLp_two` (line 1728) — This Lindeberg-tail convergence helper is public but is referenced nowhere in the entire repo (zero in-file and zero cross-file uses); it is dead proof scaffolding whose accidental external use could lock in an internal lemma, so it should be private.
  - rule: AGENTS.md PR-gate keep-same-file-scaffolding-private
  - evidence: `rg -l 'lindeberg_norm_sq_tail_normalized_uniformOn_finSucc_tendsto_zero_of_identDistrib_memLp_two' --glob '*.lean'` returns only HansenEconometrics/Chapter10Bootstrap/Variance.lean. `rg -n` returns exactly one line: `Variance.lean:1729: lindeberg_norm_sq_tail_normalized_uniformOn_finSucc_tendsto_zero_of_identDistrib_memLp_two` — the declaration itself. Source at line 1728 reads `theorem` (public), not `private theorem`. Docstring describes it as "Lindeberg tail convergence for the normalized ordinary-bootstrap one-draw summands" — internal infrastructure.
  - suggested fix: Add the `private` keyword to the theorem (`private theorem lindeberg_norm_sq_tail_normalized_uniformOn_finSucc_tendsto_zero_of_identDistrib_memLp_two`).
  - mechanical: true | confidence: high
- **[nit]** `lindeberg_norm_sq_tail_normalized_uniformOn_finSucc_le_scaled_fourth` (line 1645) — This finite Lindeberg-inequality helper is public but used only inside Variance.lean as a proof stepping stone; per the hygiene rubric a locally-only helper should be private.
  - rule: AGENTS.md PR-gate keep-same-file-scaffolding-private
  - evidence: `rg -l 'lindeberg_norm_sq_tail_normalized_uniformOn_finSucc_le_scaled_fourth' --glob='*.lean'` returns only HansenEconometrics/Chapter10Bootstrap/Variance.lean. In-file refs: line 1645 (declaration, begins `theorem ...`, no `private`) and line 1763 (sole internal use). For contrast, the adjacent helper at line 1766 is `private theorem inv_sq_mul_add_one_lt_of_div_add_one_le`. Chapter-facing theorems in this file use the `chapter10_*` prefix (e.g. lines 43, 72, 1058); this helper does not. AGENTS.md line 14: "Keep same-file proof scaffolding private." AGENTS.md lines 98-99: "A helper whose removal would only break the file where it is defined should usually be `private`."
  - suggested fix: Add the `private` keyword to the theorem declaration.
  - mechanical: true | confidence: medium
- **[nit]** `integral_tail_sq_le_inv_sq_mul_integral_fourth` (line 1587) — This fourth-moment tail-domination helper is public but used only inside Variance.lean; it is proof scaffolding that should be private (compare the sibling helper integral_tail_sq_eq_zero_of_abs_le_lt at line 1568 which is already private).
  - rule: AGENTS.md PR-gate keep-same-file-scaffolding-private
  - evidence: `rg -l 'integral_tail_sq_le_inv_sq_mul_integral_fourth' --glob '*.lean'` returns only HansenEconometrics/Chapter10Bootstrap/Variance.lean (no external files). `rg -n` shows 4 in-file occurrences: declaration at line 1587 (`theorem integral_tail_sq_le_inv_sq_mul_integral_fourth` — no `private`), and call sites at lines 1680, 3897, 4623 — all inside the same file. Sibling at line 1568 reads `private theorem integral_tail_sq_eq_zero_of_abs_le_lt`. AGENTS.md line 14: "Keep same-file proof scaffolding private."; lines 98-99: "A helper whose removal would only break the file where it is defined should usually be `private`."
  - suggested fix: Add the `private` keyword to the theorem declaration.
  - mechanical: true | confidence: medium
- **[nit]** `integral_tail_sq_matrixContinuousLinearMap_coord_le_norm_fourth` (line 5340) — This coordinate tail-integral bound helper is public but used only inside Variance.lean as proof infrastructure; per the hygiene rubric a locally-only helper should be private.
  - rule: AGENTS.md PR-gate keep-same-file-scaffolding-private
  - evidence: `rg -l 'integral_tail_sq_matrixContinuousLinearMap_coord_le_norm_fourth' --glob='*.lean'` returns only HansenEconometrics/Chapter10Bootstrap/Variance.lean. `rg -n` shows 5 occurrences all in that file: the declaration at line 5340 (`theorem integral_tail_sq_matrixContinuousLinearMap_coord_le_norm_fourth`, public, no `private`) and call sites at lines 5529, 5605, 7682, 7791. Source at line 5340 confirms it is declared as a plain public `theorem`. It is a coordinate-projection squared-tail-integral bound feeding the exact-linearization face of Theorem 10.10, i.e. internal infrastructure rather than a chapter10_* result.
  - suggested fix: Add the `private` keyword to the theorem declaration.
  - mechanical: true | confidence: medium
- **[nit]** `smoothFunctionVarianceFunctional_continuous` (line 28) — This continuity helper for the plug-in covariance map is public but used only inside Variance.lean (to discharge a continuousAt premise); it is proof scaffolding that should be private.
  - rule: AGENTS.md PR-gate keep-same-file-scaffolding-private
  - evidence: `rg -l 'smoothFunctionVarianceFunctional_continuous' --glob='*.lean'` => only `HansenEconometrics/Chapter10Bootstrap/Variance.lean`. The decl is currently public: line 28 `theorem smoothFunctionVarianceFunctional_continuous`. Its only in-file consumer is line 65: `... hPstar hGV smoothFunctionVarianceFunctional_continuous.continuousAt`, discharging the continuousAt premise of `chapter10_bootstrap_smooth_variance_consistency` (lines 43-65). AGENTS.md line 14 "Keep same-file proof scaffolding private" and lines 98-99 confirm the rule.
  - suggested fix: Add the `private` keyword to the theorem declaration.
  - mechanical: true | confidence: medium

## HansenEconometrics/Chapter10Bootstrap/WLLN.lean

### redundancy

- **[minor]** `bootstrapTailProbIndexed_zero_le_l2_eLpNorm_bound` (line 1100) — This indexed conditional-Markov bridge re-proves the full ~40-line eLpNorm Markov argument verbatim instead of delegating to the already-proved non-indexed `bootstrapTailProb_zero_le_l2_eLpNorm_bound`, even though an in-file sibling shows the delegation works.
  - rule: AGENTS.md §4 bridge-lemmas-delegate / §2 reuse-repo-theorems
  - evidence: Lines 1108-1140 (indexed) and 646-678 (non-indexed) contain identical htail (meas_ge_le_mul_pow_eLpNorm_enorm), hset (dist_eq_norm/ofReal_norm_eq_enorm), hmeasure, hrhs_ne_top (finiteness via eLpNorm_ne_top), and hreal (ENNReal.toReal_mono) blocks; only the setOf binder type differs (Ωboot n vs Ωs). bootstrapL2ENNTailBoundIndexed (1090-1096) = ((ENNReal.ofReal η)⁻¹^2 * eLpNorm (Zstar n ω) 2 (Pstar n ω)^2).toReal, identical to bootstrapL2ENNTailBound (631-636). Both Markov lemmas take only (hZ : ∀ n ω, MemLp ...) and (hη : 0 < η) — no hPstar — so the sibling's delegation pattern (Pconst := fun _ _ => Pstar n ω; Zconst := fun _ _ ωs => Zstar n ω ωs; call non-indexed lemma; simpa) at lines 1627-1634 transfers directly. Usage check: `rg -l 'bootstrapTailProbIndexed_zero_le_l2_eLpNorm_bound'` returns only WLLN.lean; the sole caller is in-file at line 1209, so the fixer rewrites the proof body in place rather than deleting the decl — no external callers exist.
  - suggested fix: Replace the duplicated proof body with the `Pconst`/`Zconst` constant-instantiation delegation used by `bootstrapTailProbIndexed_zero_le_integral_norm_sq_div`, calling `bootstrapTailProb_zero_le_l2_eLpNorm_bound` and closing with `simpa [bootstrapTailProbIndexed, bootstrapTailProb, bootstrapL2ENNTailBoundIndexed, bootstrapL2ENNTailBound, Pconst, Zconst]`.
  - mechanical: false | confidence: high
- **[minor]** `bootstrapTailProbIndexed_centered_real_le_variance_div_sq` (line 1057) — This indexed conditional-Chebyshev bridge re-proves the full variance/Markov argument verbatim instead of delegating to the already-proved non-indexed `bootstrapTailProb_centered_real_le_variance_div_sq`.
  - rule: AGENTS.md §4 bridge-lemmas-delegate / §2 reuse-repo-theorems
  - evidence: Indexed proof WLLN.lean:1066-1087 vs non-indexed WLLN.lean:741-761 are step-for-step identical: `haveI : IsProbabilityMeasure (Pstar n ω) := hPstar n ω`; `hcheb := ProbabilityTheory.meas_ge_le_variance_div_sq ... (hZ n ω) hη`; `hset` closed by `simp [hmean n ω]`; `hmeasure` via `rw [hset]; exact hcheb`; `hnonneg := div_nonneg (ProbabilityTheory.variance_nonneg ...) (sq_nonneg η)`; `hreal := ENNReal.toReal_mono ENNReal.ofReal_ne_top hmeasure`; close `simpa [bootstrapTailProb(Indexed), ENNReal.toReal_ofReal hnonneg] using hreal`. Delegation template proven in sibling bootstrapTailProbIndexed_zero_le_integral_norm_sq_div (WLLN.lean:1627-1634): `let Pconst := fun _ _ => Pstar n ω; let Zconst := fun _ _ ωs => Zstar n ω ωs; ... simpa [bootstrapTailProbIndexed, bootstrapTailProb, Pconst, Zconst] using htail`. Defs identical modulo space: bootstrapTailProb (BootstrapUtils.lean:53-54) and bootstrapTailProbIndexed (WLLN.lean:1014-1015) both `= ((Pstar n ω) {ωs | η ≤ dist (Zstar n ω ωs) (Z ω)}).toReal`. Caller check: `rg -l bootstrapTailProbIndexed_centered_real_le_variance_div_sq` returns only HansenEconometrics/Chapter10Bootstrap/WLLN.lean (zero external callers) — the fixer can safely rewrite the proof body in place; the lemma statement itself should be retained.
  - suggested fix: Mirror `bootstrapTailProbIndexed_zero_le_integral_norm_sq_div`: introduce `Pconst := fun _ _ => Pstar n ω` and `Zconst := fun _ _ ωs => Zstar n ω ωs`, apply `bootstrapTailProb_centered_real_le_variance_div_sq` with `hPconst`/`hZconst`/`hmean`, and close with `simpa [bootstrapTailProbIndexed, bootstrapTailProb, Pconst, Zconst]`.
  - mechanical: false | confidence: high

### hygiene

- **[nit]** `bootstrapTailProbIndexed_zero_le_integral_norm_sq_div` (line 1618) — Public bridge theorem is used only inside WLLN.lean and is not a chapter-facing crosswalk entry, so per 'keep same-file proof scaffolding private' it should be private.
  - rule: AGENTS.md keep-scaffolding-private
  - evidence: `rg -l 'bootstrapTailProbIndexed_zero_le_integral_norm_sq_div' --glob='*.lean'` returns only HansenEconometrics/Chapter10Bootstrap/WLLN.lean. `rg -n` shows: line 1618 (decl, declared as plain `theorem`, not `private theorem`), and call sites at 2325, 2471, 2549, 2805 — all in WLLN.lean. The declaration name does not appear in inventory/ch10-inventory.md (grep returned nothing). Proof body (lines 1627-1634) builds `Pconst`/`Zconst` constant families and `simpa ... using` the public `bootstrapTailProb_zero_le_integral_norm_sq_div` (defined line 687). AGENTS.md lines 14, 98-99 supply the applicable rule.
  - suggested fix: Add the `private` modifier to the theorem.
  - mechanical: true | confidence: medium
- **[nit]** `bootstrapWLLNSecondMomentBound_succ_tendsto_zero` (line 1640) — Public shifted-index helper theorem is used only inside WLLN.lean and is not a chapter-facing crosswalk entry, so per 'keep same-file proof scaffolding private' it should be private.
  - rule: AGENTS.md keep-scaffolding-private
  - evidence: rg -n across *.lean: the name appears only in HansenEconometrics/Chapter10Bootstrap/WLLN.lean — definition at line 1640 (`theorem bootstrapWLLNSecondMomentBound_succ_tendsto_zero`, no `private` modifier), and internal call sites `exact bootstrapWLLNSecondMomentBound_succ_tendsto_zero` at lines 2503 and 2784. `rg -l` returns only WLLN.lean (zero external files). The proof body (lines 1646-1651) composes `bootstrapWLLNSecondMomentBound_tendsto_zero` with `tendsto_add_atTop_nat 1`. Inventory grep for `bootstrapWLLNSecondMomentBound_succ` in inventory/ch10-inventory.md exits 1 (not present), while the public base lemmas `bootstrapWLLNSecondMomentBound` and `bootstrapWLLNSecondMomentBound_tendsto_zero` ARE listed at lines 794-795. AGENTS.md line 14: '- Keep same-file proof scaffolding private.'
  - suggested fix: Add the `private` modifier to the theorem.
  - mechanical: true | confidence: medium

### proof-quality

- **[minor]** `bootstrapWLLNSecondMomentBound_tendsto_zero` (line 611) — The hypothesis `(_hη : 0 < η)` is a dead hypothesis: it is never used in the proof body, making the theorem needlessly stronger than necessary.
  - rule: AGENTS.md Theorem-writing policy: state assumptions only as strongly as needed
  - evidence: Signature line 613: `(hu : UniformIntegrable u 1 μ) {η : ℝ} (_hη : 0 < η) :`. Proof body lines 616-623 use `chapter10_marcinkiewicz_wlln_natPower_of_uniformIntegrable ... hu` and `TendstoInMeasure.const_mul_zero_real (μ := μ) (η⁻¹ ^ 2) hmarc`; `_hη` is not referenced. `rg -n '_hη' WLLN.lean` returns a single match (line 613, the binder), proving the dead hypothesis is never used. Note: the suggested_fix's call-site claim (lines 784/1183/1647) is outside the scope of this dead-hypothesis verdict but does not affect the validity of the core claim.
  - suggested fix: Remove the `(_hη : 0 < η)` hypothesis from the statement and update the three in-file call sites (lines 784, 1183, 1647) to stop passing `hη`.
  - mechanical: false | confidence: high

## HansenEconometrics/Chapter10Bootstrap/WeakDistribution.lean

### hygiene

- **[major]** `WeakDistribution` (line 1) — This 6974-line module — the largest file in Chapter 10 with 175+ public declarations spanning bootstrap weak convergence, Portmanteau sandwiches, coordinate-orthant frontier nullity, the Gaussian CLT capstones, and continuous-mapping theorems — has no module-level docstring explaining its contents or public surface; the file jumps straight from imports/opens to `namespace HansenEconometrics` and the first per-declaration docstring at line 17.
  - rule: AGENTS.md PR-gate: docstrings-for-large-modules
  - evidence: `rg -n '/-!' WeakDistribution.lean` returns no matches (no `/-! ... -/` module doc block anywhere). `wc -l` = 6974 (exactly matches the claim). Public declaration count = 181 (rg -c on theorem/lemma/def/structure/class/instance/abbrev), consistent with the claimed 175+. Lines 1-8 are imports (`import HansenEconometrics.Chapter10Bootstrap.Distribution`, `...WLLN`), `open`/`open scoped` lines, then `namespace HansenEconometrics`; the first `/--` docstring at line 17 is a per-declaration docstring for `bootstrapBoundedContinuousIntegral`, not a module-level doc.
  - suggested fix: Add a module-level `/-! # Bootstrap weak convergence ... -/` doc block immediately after the imports/opens (before `namespace HansenEconometrics`) summarizing the file's sections (bounded-continuous integral backend, indexed variants, sandwich/Portmanteau bridges, coordinateLE frontier-nullity lemmas, Gaussian CLT capstones `chapter10_*`, continuous-mapping theorems) and naming the chapter-facing public API.
  - mechanical: true | confidence: high

## Draft fixes

51 mechanical finding(s) sent to 15 fixer worktree(s). Per-file outcomes:

```
Confirmed. This worktree's HEAD is `0227417`, which is the merge-base with `review-harness` — meaning the worktree is checked out at a commit far behind `review-harness`. The target file `HansenEconometrics/Chapter10Bootstrap/Covariance.lean` (introduced in commit `287b6c7` on the `review-harness`/`codex/chapter-10-complete` branches) does not exist in this worktree, nor does the `review/` directory containing my instructions.

I cannot apply any of the three mechanical edits because there is no file to edit. Downgrading all findings to report-only.

[{"id":"8f34d4a721b0a74858e4093824a6b3dc464ceb7c","decl":"<module>","status":"report_only","reason":"Target file HansenEconometrics/Chapter10Bootstrap/Covariance.lean does not exist in this worktree. The worktree HEAD is 0227417 (the merge-base with review-harness), which predates commit 287b6c7 that introduced the file. The review/ harness directory is also absent. No edit possible."},{"id":"a9cec91a33a6135479d57c9051db96af15d7bbee","decl":"abs_dotProduct_le_l1_mul_norm","status":"report_only","reason":"Target file does not exist in this worktree (HEAD 0227417 predates the file's introduction in 287b6c7). No edit possible."},{"id":"bcdb42e5de3c386a6f369e1e79edfea08975735c","decl":"measure_strict_norm_gt_le_inv_sq_mul_integral_norm_sq","status":"report_only","reason":"Target file does not exist in this worktree (HEAD 0227417 predates the file's introduction in 287b6c7). No edit possible."}]

ENVIRONMENT ERROR: This worktree was created from the wrong base. Its HEAD is 0227417 (merge-base with review-harness), so neither the target file HansenEconometrics/Chapter10Bootstrap/Covariance.lean (added in commit 287b6c7) nor the review/ harness directory exist here. All three findings are downgraded to report_only — no edits applied, build not run (no file to build/affect). To fix: re-create the fixer worktree from the review-harness branch (af5e4c5) where both the target file and review/prompts/fixer.md are present.
```
```
The file existed historically (created in commit `287b6c7`) and exists in the main checkout, but it does NOT exist at the worktree's HEAD (`0227417`). The worktree is checked out at a commit that predates (or is on a divergent branch from) the "Split Chapter 10 bootstrap module" commit that created this file.

Since the target file does not exist in this worktree, I cannot apply any of the mechanical edits. The fixer instructions are clear: never edit outside the worktree, and never touch a file that isn't present. Per the "when in doubt, do nothing" principle, every finding must be downgraded to report-only.

I'll return all 8 findings as `report_only` with the reason that the target file is absent at the worktree HEAD.

[{"status":"report_only","decl":"chapter10_bootstrap_smooth_function_gaussian_of_compact_range_closeness","reason":"forbidden edit type — target file HansenEconometrics/Chapter10Bootstrap/DeltaMethod.lean does not exist at the worktree HEAD (0227417). The file (and the smooth_function duplicate) only exists in the main checkout / a later commit (287b6c7), not in this isolated worktree. No edit could be applied without touching a non-existent file, so per 'when in doubt, do nothing' this is downgraded to report-only. No build was attempted.","build_error":null},{"status":"report_only","decl":"chapter10_bootstrap_smooth_function_gaussian_of_compact_tail_closeness","reason":"forbidden edit type — target file does not exist at worktree HEAD (0227417); cannot apply remove-duplicate edit.","build_error":null},{"status":"report_only","decl":"chapter10_bootstrap_smooth_function_gaussian_event_probability_of_compact_tail_closeness","reason":"forbidden edit type — target file does not exist at worktree HEAD (0227417); cannot apply remove-duplicate edit.","build_error":null},{"status":"report_only","decl":"chapter10_bootstrap_smooth_function_gaussian_distribution_of_compact_tail_closeness","reason":"forbidden edit type — target file does not exist at worktree HEAD (0227417); cannot apply remove-duplicate edit.","build_error":null},{"status":"report_only","decl":"chapter10_bootstrap_smooth_function_gaussian_event_probability_of_compact_range_closeness","reason":"forbidden edit type — target file does not exist at worktree HEAD (0227417); cannot apply remove-duplicate edit.","build_error":null},{"status":"report_only","decl":"chapter10_bootstrap_smooth_function_gaussian_distribution_of_compact_range_closeness","reason":"forbidden edit type — target file does not exist at worktree HEAD (0227417); cannot apply remove-duplicate edit.","build_error":null},{"status":"report_only","decl":"module","reason":"forbidden edit type — target file HansenEconometrics/Chapter10Bootstrap/DeltaMethod.lean does not exist at worktree HEAD (0227417); cannot add module docstring to a non-existent file.","build_error":null}]
```
```
Confirmed. The target file `HansenEconometrics/Chapter10Bootstrap/Distribution.lean` exists only on the `codex/chapter-10-complete` branch, not on the branch this worktree was checked out from (`main` @ `0227417`). The worktree was created against the wrong base branch, so the file — and the `review/` prompts directory — are entirely absent here.

I cannot apply any edits because there is no target file to edit. No build was run (no change to test), and the working tree remains clean. Both findings are downgraded to report-only.

[{"id":"25af06f5771ae6aadb83b23c8722f08080f770f4","file":"HansenEconometrics/Chapter10Bootstrap/Distribution.lean","decl":"TendstoInBootstrapDistribution.congr","outcome":"report_only","reason":"Target file does not exist in this worktree. The worktree is on branch worktree-wf_0bd2c931-47a-128 (branched from main @ 0227417), which has no HansenEconometrics/Chapter10Bootstrap/ directory. The file exists only on branch codex/chapter-10-complete. No edit could be applied; working tree left clean."},{"id":"fb3df2c373d55acfa5b698593bbb97b53d0004f1","file":"HansenEconometrics/Chapter10Bootstrap/Distribution.lean","decl":"TendstoInBootstrapDistributionIndexed.congr","outcome":"report_only","reason":"Target file does not exist in this worktree (same root cause: worktree branched from main, which lacks Chapter10Bootstrap/; file lives on codex/chapter-10-complete). No edit applied; working tree left clean."}]

Note to orchestrator: this worktree appears to have been created from the wrong base branch (main instead of codex/chapter-10-complete). The review/ prompts directory and the entire HansenEconometrics/Chapter10Bootstrap/ directory are absent here, so no mechanical fix was possible. No lake build was run because no files changed.
```
```
Confirmed. The target file does not exist anywhere in this worktree. The worktree was created from commit `0227417`, which predates the creation of `HansenEconometrics/Chapter10Bootstrap/Empirical.lean`. None of the four mechanical edits can be applied because there is no file to edit, and per the fixer protocol I must never create or substantially rewrite files — only apply narrow edits to an existing target.

All four findings are downgraded to report_only.

[{"id":"50b54898c0e6a63ea649dc6458e88617f4bd10dc","decl":"<module>","status":"report_only","reason":"Target file HansenEconometrics/Chapter10Bootstrap/Empirical.lean does not exist in this worktree. The worktree branch HEAD is commit 0227417, which predates the creation of the Chapter10Bootstrap directory (the file exists only in the main working copy). No edit could be applied and no build was run."},{"id":"9f3e8d5011b9b7dff28afc78e5df6d77f1612630","decl":"centeredEmpiricalCharFunFinSucc_remainder_norm_le_integral_split","status":"report_only","reason":"Target file HansenEconometrics/Chapter10Bootstrap/Empirical.lean does not exist in this worktree (branch HEAD 0227417 predates the Chapter10Bootstrap directory). No edit applied."},{"id":"0dcf0d52026d27d7610218810df9e25a3800f02f_scaled","decl":"centeredEmpiricalCharFunFinSucc_remainder_isLittleO_of_scaled_norm_tendsto_zero","status":"report_only","reason":"Target file HansenEconometrics/Chapter10Bootstrap/Empirical.lean does not exist in this worktree (branch HEAD 0227417 predates the Chapter10Bootstrap directory). No edit applied."},{"id":"0dcf0d52026d27d7610218810df9e25a3800f02f","decl":"centeredEmpiricalCharFunFinSucc_remainder_isLittleO_of_variance_bound_tail","status":"report_only","reason":"Target file HansenEconometrics/Chapter10Bootstrap/Empirical.lean does not exist in this worktree (branch HEAD 0227417 predates the Chapter10Bootstrap directory). No edit applied."}]

SUMMARY: 0 applied, 4 report_only. Blocking reason: the target file `HansenEconometrics/Chapter10Bootstrap/Empirical.lean` is absent from this worktree because the worktree branch (`worktree-wf_0bd2c931-47a-129`) was created from commit `0227417`, which predates the Chapter10Bootstrap directory. The file exists only in the main working copy (`/Users/psg24/repos/lean-hansen-econometrics/HansenEconometrics/Chapter10Bootstrap/Empirical.lean`). No edits were made and `lake build` was not run. The worktree must be re-created from a base commit that contains the target file.
```
```
The file exists in the repo's history (commit `287b6c7`) but **not** in this worktree's branch (`worktree-wf_0bd2c931-47a-130`), which is based on an older commit (`0227417`) that predates Chapter 10 entirely. The `review/` directory is also absent.

This worktree was created from the wrong base commit. The target file does not exist here, so I cannot apply the mechanical edit. Per the fixer protocol, I must not fabricate or create the file, and I cannot edit a file that isn't present. I'll report this finding as report_only.

[{"id":"2b6baf2dba0d9ef8436ba30d855faac93d61c84a","file":"HansenEconometrics/Chapter10Bootstrap/FiniteReplication.lean","status":"report_only","reason":"Target file does not exist in this worktree. The worktree (branch worktree-wf_0bd2c931-47a-130) is based on commit 0227417 (HEAD predates Chapter 10), so neither HansenEconometrics/Chapter10Bootstrap/FiniteReplication.lean nor the review/ directory is present. The file only appears later in repo history (commit 287b6c7 'Split Chapter 10 bootstrap module'). No mechanical edit could be applied because there is no file to edit; creating the module or its docstring from scratch is out of scope for a mechanical fixer. No changes were made; build was not run (nothing to rebuild). The worktree appears to have been spawned from an incorrect base commit relative to the finding."}]
```
```
The target file `HansenEconometrics/Chapter10Bootstrap/HigherOrder.lean` does not exist in this worktree. There is no `Chapter10Bootstrap` directory at all. This worktree is checked out at commit `0227417`, which predates the existence of the target file.

Since the target file does not exist, the mechanical edit cannot be applied. I'll downgrade to report-only.

```json
[
  {
    "status": "report_only",
    "decl": "HigherOrder",
    "reason": "Target file HansenEconometrics/Chapter10Bootstrap/HigherOrder.lean does not exist in this worktree. The worktree is checked out at commit 0227417, which has no Chapter10Bootstrap directory; the file is absent so the add_docstring edit cannot be applied.",
    "build_error": null
  }
]
```
```
```
The file exists on other branches/commits but not on this worktree's checked-out base (0227417). The worktree is positioned at a commit that predates the target file, so I cannot apply the mechanical edits as specified — the file is not present on disk and its expected line numbers do not exist here. Making changes would require checking out a different commit, which is outside the mechanical-fixer mandate and risks operating on a tree that doesn't match the findings.

All three findings are downgraded to report-only.

[{"status":"report_only","decl":"<module>","reason":"lake build failed — target file not present in worktree; the worktree base commit 0227417 predates HansenEconometrics/Chapter10Bootstrap/Percentile.lean, so the file does not exist on disk at /Users/psg24/repos/lean-hansen-econometrics/.claude/worktrees/wf_0bd2c931-47a-132/HansenEconometrics/Chapter10Bootstrap/Percentile.lean (the Chapter10Bootstrap directory is absent). No edit could be applied. Checking out a different ref is outside the mechanical-fixer mandate and would not match the findings' line numbers.","build_error":null},{"status":"report_only","decl":"frontier_percentileCoverageSet_subset","reason":"lake build failed — target file not present in worktree; base commit 0227417 predates HansenEconometrics/Chapter10Bootstrap/Percentile.lean, so the file is absent from disk and no edit could be applied.","build_error":null},{"status":"report_only","decl":"percentileCoverage_frontier_null_of_boundary_null","reason":"lake build failed — target file not present in worktree; base commit 0227417 predates HansenEconometrics/Chapter10Bootstrap/Percentile.lean, so the file is absent from disk and no edit could be applied.","build_error":null}]
```
```
Confirmed. The worktree is checked out at commit `0227417`, and the target file `HansenEconometrics/Chapter10Bootstrap/PercentileT.lean` does not exist in that commit's tree (nor anywhere in this branch's working copy). The file the findings reference exists only on the `review-harness` branch in the main checkout.

I cannot apply any of the three mechanical edits because there is no target file to edit in this worktree. Reverting to report-only for all findings is the correct, safe outcome. I made no changes.

[{"status":"report_only","decl":"<module>","reason":"lake build not attempted: target file HansenEconometrics/Chapter10Bootstrap/PercentileT.lean does not exist in this worktree. The worktree is checked out at commit 0227417 (branch worktree-wf_0bd2c931-47a-133), whose tree contains no Chapter10Bootstrap directory at all. The file the findings target exists only on the review-harness branch in the main checkout, not on this worktree's base commit. No edit could be applied to a nonexistent file.","build_error":null},{"status":"report_only","decl":"percentileTCoverageVector_mem_set_iff","reason":"lake build not attempted: target file HansenEconometrics/Chapter10Bootstrap/PercentileT.lean does not exist in this worktree (HEAD 0227417 has no Chapter10Bootstrap directory). Orchestration mismatch — worktree base commit predates the target file. No edit applied.","build_error":null},{"status":"report_only","decl":"percentileTCIEvent_iff_tstat_between","reason":"lake build not attempted: target file HansenEconometrics/Chapter10Bootstrap/PercentileT.lean does not exist in this worktree (HEAD 0227417 has no Chapter10Bootstrap directory). Finding was also low-confidence (possible chapter-facing API). No edit applied.","build_error":null}]
```
```
The target file exists only on commit `287b6c7` ("Split Chapter 10 bootstrap module"), which is on the `codex/chapter-10-complete` branch — not on the branch this worktree was created from. The worktree branch `worktree-wf_0bd2c931-47a-134` was forked from `0227417` (the current main-ish HEAD), which has neither the target file nor the `review/` harness.

This means the fixer was spawned against the wrong base: the target file `HansenEconometrics/Chapter10Bootstrap/Quantiles.lean` is not present in this worktree, so there is nothing to edit and no `lake build` to gate. I cannot apply any of the 7 mechanical findings.

Per the report-only fallback principle in my instructions, I downgrade every finding to `report_only` (build/edit not possible because the target file is absent from this worktree's checkout). I made no changes to any file.

[{"id":"826664ab09287c9eb54be0fb5766b6cb3339e7da","outcome":"report_only","reason":"Target file HansenEconometrics/Chapter10Bootstrap/Quantiles.lean does not exist in this worktree. The worktree branch worktree-wf_0bd2c931-47a-134 was forked from commit 0227417, which does not contain Chapter10Bootstrap/. The file exists only on commit 287b6c7 ('Split Chapter 10 bootstrap module', branch codex/chapter-10-complete). No edit applied; cannot run lake build against a missing file."},{"id":"efa0b7e8a4f39dc1a95a4e073f4431381b4eeeb9","outcome":"report_only","reason":"Target file absent from this worktree (see above). No edit applied."},{"id":"f64b5f436bc64b747f02970e433e547fcffb406c","outcome":"report_only","reason":"Target file absent from this worktree. No edit applied."},{"id":"911750eafb928a2d08f6f967a89f44212ab6fd33","outcome":"report_only","reason":"Target file absent from this worktree. No edit applied."},{"id":"62cb08156e1c8abc082c191bc6346a8d83f7232d","outcome":"report_only","reason":"Target file absent from this worktree. No edit applied."},{"id":"9584717120a73c45496a00ba85a5854f059a8222","outcome":"report_only","reason":"Target file absent from this worktree. No edit applied."},{"id":"9d0081893bf626eef12664529a59eff385491ccd","outcome":"report_only","reason":"Target file absent from this worktree. No edit applied."}]

BLOCKER: This worktree (branch worktree-wf_0bd2c931-47a-134, forked from 0227417) does not contain the target file HansenEconometrics/Chapter10Bootstrap/Quantiles.lean, nor the review/ harness directory, nor review/prompts/fixer.md. The file exists only on commit 287b6c7 (branch codex/chapter-10-complete). No mechanical edits could be applied and no lake build was run. All 7 findings downgraded to report_only. The orchestrator likely forked the fixer worktree from the wrong base commit/branch — re-spawn from the commit/branch that contains Chapter10Bootstrap/Quantiles.lean (e.g. 287b6c7 / codex/chapter-10-complete).
```
```
Confirmed. The target file `HansenEconometrics/Chapter10Bootstrap/Regression.lean` exists only on `codex/chapter-10-complete`, not on this worktree's branch (which is based off `main` at commit `0227417`). The file does not exist in this worktree, so none of the mechanical edits can be applied here.

This is an environment/setup mismatch: the worktree was created from the wrong base branch. The fixer instructions forbid touching files outside the single target file, and I must not switch branches or pull in the file from another branch (that would be a non-mechanical, multi-file structural change). The correct action is to downgrade every finding to `report_only`.

```json
[{"id":"5eca4588696484b06391837f6cab9f7ac46c9e9b","decl":"<module>","status":"report_only","reason":"Target file HansenEconometrics/Chapter10Bootstrap/Regression.lean does not exist in this worktree. The worktree branch (worktree-wf_0bd2c931-47a-135) is based on main@0227417, where the Chapter10Bootstrap directory does not exist; the file exists only on branch codex/chapter-10-complete. No edit applied."},{"id":"a4ce160843b80617c84d498cbc4f275d4cab9845","decl":"regressionBootstrap_sampleCrossMoment_errors_finSucc_eq_resampleMean","status":"report_only","reason":"Target file does not exist in this worktree (wrong base branch — main@0227417 lacks Chapter10Bootstrap/Regression.lean). No edit applied."},{"id":"3b8c8c48e5ecb1def07fe3845fe42ba2050c1ee7","decl":"sampleCrossMoment_stackRegressors_stackErrors_finSucc_eq_empiricalMean","status":"report_only","reason":"Target file does not exist in this worktree (wrong base branch — main@0227417 lacks Chapter10Bootstrap/Regression.lean). No edit applied."},{"id":"e53b6445bd9002fc590ed64b5629a29d68afe9e8","decl":"regressionBootstrapScoreFinSucc_eq_sqrt_smul_sampleCrossMoment_sub","status":"report_only","reason":"Target file does not exist in this worktree (wrong base branch — main@0227417 lacks Chapter10Bootstrap/Regression.lean). No edit applied."},{"id":"7eee239f671812eaf693c4a45be06a921399ac0d","decl":"regressionBootstrapLinearRestrictionStatisticFinSucc_eq_theta_apply","status":"report_only","reason":"Target file does not exist in this worktree (wrong base branch — main@0227417 lacks Chapter10Bootstrap/Regression.lean). No edit applied."},{"id":"09484e47c765b456f456012899de8396633a5d41","decl":"regressionBootstrapLinearRestrictionStatisticFinSucc_abs_le_beta_norm","status":"report_only","reason":"Target file does not exist in this worktree (wrong base branch — main@0227417 lacks Chapter10Bootstrap/Regression.lean). No edit applied."}]
```

All 6 findings downgraded to report_only. Root cause: the target file `/Users/psg24/repos/lean-hansen-econometrics/.claude/worktrees/wf_0bd2c931-47a-135/HansenEconometrics/Chapter10Bootstrap/Regression.lean` does not exist in this worktree. The worktree branch is based on `main` (commit `0227417`), where the `Chapter10Bootstrap` directory is absent. The file exists only on branch `codex/chapter-10-complete`. No edits were applied and the worktree is unchanged.
```
```
The finding's claim is accurate (2180-line file, no `/-!` module docstring), but I cannot apply the edit: the target file does not exist in my assigned worktree.

f023c0a5194b5903aac501f368ed1465277afa08: report_only

Reason: The target file is absent from this worktree. The worktree's branch `worktree-wf_0bd2c931-47a-136` has HEAD at commit `0227417` (Merge PR #48 / Chapter 8 era), which PREDATES the creation of `HansenEconometrics/Chapter10Bootstrap/Studentization.lean`. The file was introduced later in commit `287b6c7` ("Split Chapter 10 bootstrap module"), which is NOT an ancestor of this worktree's HEAD. The entire `HansenEconometrics/Chapter10Bootstrap/` directory and the `review/` harness (prompts/finding-schema.json) are also missing here.

This is a worktree-provisioning bug: the worktree was based on the wrong commit. I cannot apply the mechanical docstring edit (would require checking out/recreating a file from a different repo state — far outside the "single target file, narrow pre-approved edit" scope, and `lake build` could not validate it against the missing Chapter 10 dependency tree anyway). No edits were made; the worktree is unchanged.

Finding validity (verified against historical content of the file at `287b6c7`, read-only, no worktree modification): CONFIRMED. `git show 287b6c7:.../Studentization.lean | wc -l` = 2180 lines; `rg -c '/-!'` = 0 (no module-level docstring); file begins directly with `import HansenEconometrics.Chapter7Asymptotics.Inference`. The claim that this large module lacks the AGENTS.md-required module docstring (AGENTS.md line 18) holds. The suggested fix remains valid and should be applied in a worktree based on a commit that actually contains the file (e.g., current `main`).

Outcome: 1 finding, 0 applied, 1 report_only (blocked by missing target file / mis-provisioned worktree base commit).
```
```
The target file does not exist on this worktree's branch, so none of the mechanical edits can be applied.

The worktree is on branch `worktree-wf_0bd2c931-47a-137` (derived from `main`), which does not contain the Chapter 10 bootstrap module. The target file `HansenEconometrics/Chapter10Bootstrap/Tests.lean` only exists on the `codex/chapter-10-complete` branch (committed in `287b6c7 Split Chapter 10 bootstrap module`). On my branch there is no `HansenEconometrics/Chapter10Bootstrap/` directory at all (`git ls-files | grep -i chapter10` returns nothing).

I cannot apply any edits to a file that is not present on my branch, and per the fixer constraint I must never create or edit files outside the single confirmed target. No build was run because there is nothing to change. All findings are reported as report_only due to this environment/setup mismatch (wrong base branch checked out for the worktree).

[
{"id":"03986cfda476ccf9de483da7fedd27f9256a500f","outcome":"report_only","reason":"Target file HansenEconometrics/Chapter10Bootstrap/Tests.lean does not exist on this worktree's branch (worktree-wf_0bd2c931-47a-137, based on main). The Chapter10Bootstrap directory is absent; the file only exists on branch codex/chapter-10-complete. No edit possible."},
{"id":"442127f4d123b717786763dcab1b156f1a86e401","outcome":"report_only","reason":"Target file absent on this branch (no HansenEconometrics/Chapter10Bootstrap/ directory). Branch mismatch: file lives only on codex/chapter-10-complete. No edit possible."},
{"id":"0e0edaea86aa3946c1936b81bbc94d0c8b365a80","outcome":"report_only","reason":"Target file absent on this branch. Branch mismatch: file lives only on codex/chapter-10-complete. No edit possible."},
{"id":"0859fcd66497cb9be82d3b533d9df3f023cdcf67","outcome":"report_only","reason":"Target file absent on this branch. Branch mismatch: file lives only on codex/chapter-10-complete. No edit possible."},
{"id":"804ffff7e037b59ec041bdb32d3baf097fcba9f5","outcome":"report_only","reason":"Target file absent on this branch. Branch mismatch: file lives only on codex/chapter-10-complete. No edit possible."}
]

Working tree is clean; no changes were made and `lake build` was not run. Worktree path: /Users/psg24/repos/lean-hansen-econometrics/.claude/worktrees/wf_0bd2c931-47a-137 (branch worktree-wf_0bd2c931-47a-137). The orchestrator should re-create this worktree from a branch that actually contains HansenEconometrics/Chapter10Bootstrap/Tests.lean (e.g. codex/chapter-10-complete).
```
```
The target file exists in git history (commit 287b6c7 on some branch) but not on this worktree's branch `worktree-wf_0bd2c931-47a-138`, which was branched from commit 0227417 — a point before Chapter 10 existed. The worktree was created from the wrong base commit.

I cannot apply any of the mechanical edits because the target file `HansenEconometrics/Chapter10Bootstrap/Variance.lean` does not exist in this worktree, and neither does the `review/` infrastructure. Applying edits requires the file to be present. Let me report this back rather than attempting any speculative recreation.

All 5 findings must be downgraded to `report_only` because the target file is absent from this worktree's checkout.

[{"id":"e6c7d204d0f21c3dd2f2912e1558421e20a122fb","outcome":"report_only","reason":"Target file HansenEconometrics/Chapter10Bootstrap/Variance.lean does not exist in this worktree. The worktree branch (worktree-wf_0bd2c931-47a-138) was created from commit 0227417, which predates Chapter 10 entirely; the Chapter10Bootstrap directory and the review/ infrastructure are absent. No edit could be applied."},{"id":"0092837010117236742edcea22e5facb4ea41090","outcome":"report_only","reason":"Target file HansenEconometrics/Chapter10Bootstrap/Variance.lean absent from this worktree (branched from pre-Chapter-10 commit 0227417). No edit applied."},{"id":"b97d179ed421f26fe3543ad4a168c30f0e4cc4b4","outcome":"report_only","reason":"Target file HansenEconometrics/Chapter10Bootstrap/Variance.lean absent from this worktree (branched from pre-Chapter-10 commit 0227417). No edit applied."},{"id":"660f0e2d3eb0286f3d7d614266ad2da427f7d678","outcome":"report_only","reason":"Target file HansenEconometrics/Chapter10Bootstrap/Variance.lean absent from this worktree (branched from pre-Chapter-10 commit 0227417). No edit applied."},{"id":"3cef19effb1842fceea9e43fb931baf5ec044efe","outcome":"report_only","reason":"Target file HansenEconometrics/Chapter10Bootstrap/Variance.lean absent from this worktree (branched from pre-Chapter-10 commit 0227417). No edit applied."}]

ENVIRONMENT ERROR: This worktree (/Users/psg24/repos/lean-hansen-econometrics/.claude/worktrees/wf_0bd2c931-47a-138, branch worktree-wf_0bd2c931-47a-138) is checked out at commit 0227417, which predates the existence of HansenEconometrics/Chapter10Bootstrap/Variance.lean and the review/ directory. The target file is not present. No mechanical edits were applied and no build was run (lake build would not exercise the intended file). The worktree must be recreated from a base commit that contains Chapter10Bootstrap/Variance.lean (the file exists in history at commit 287b6c7 "Split Chapter 10 bootstrap module").
```
```
The target file `HansenEconometrics/Chapter10Bootstrap/WLLN.lean` does not exist at this worktree's commit (`0227417`, which is from PR #48 — Chapter 8 era). There is no Chapter10Bootstrap directory anywhere in the git tree at this checkout. The findings reference line numbers up to ~2805 in a file that does not exist here.

Both findings cannot be applied because the target file is absent. I'll report both as report-only.

[{"status":"report_only","decl":"bootstrapTailProbIndexed_zero_le_integral_norm_sq_div","reason":"Target file HansenEconometrics/Chapter10Bootstrap/WLLN.lean does not exist in this worktree. The worktree is checked out at commit 0227417 (PR #48, Chapter 8 era); the Chapter10Bootstrap directory is absent from the git tree at this checkout, so the mechanical 'make private' edit cannot be applied.","build_error":null},{"status":"report_only","decl":"bootstrapWLLNSecondMomentBound_succ_tendsto_zero","reason":"Target file HansenEconometrics/Chapter10Bootstrap/WLLN.lean does not exist in this worktree. The worktree is checked out at commit 0227417 (PR #48, Chapter 8 era); the Chapter10Bootstrap directory is absent from the git tree at this checkout, so the mechanical 'make private' edit cannot be applied.","build_error":null}]
```
```
The file exists in the main checkout and on some branch (commit `287b6c7`), but the worktree was created from commit `0227417` (a merge of PR #48 on the `review-harness` line) which predates the Chapter 10 bootstrap split. So the target file genuinely does not exist on this worktree's base commit.

This is the fixer-worktree gotcha noted in memory. The worktree was checked out at a base that does not contain the target file, so I cannot apply the mechanical edit. The correct action is to downgrade to report-only rather than fabricate or operate outside the worktree.

[{"status":"report_only","decl":"WeakDistribution","reason":"forbidden edit type | target file absent from worktree base — the file HansenEconometrics/Chapter10Bootstrap/WeakDistribution.lean does not exist on this worktree's checked-out commit (0227417, the PR #48 merge which predates commit 287b6c7 'Split Chapter 10 bootstrap module'). There is no Chapter10Bootstrap directory in this worktree at all. Applying the docstring edit is impossible without creating the file, which is out of scope for a mechanical add-docstring fix; no edit was applied and the working tree is unchanged. (The file does exist in the main checkout, so the fix should be retried in a worktree whose base contains the Chapter 10 split.)","build_error":null}]
```
