# Lean Review Report

Reviewed 2 file(s); 15 confirmed finding(s).

## HansenEconometrics/Chapter10Bootstrap/HigherOrder.lean

### hygiene

- **[minor]** `HigherOrder` (line 1) — This is a large module (895 lines, 27 public theorems forming the chapter-facing Theorem 10.15 / 10.17 second-order refinement API) with no module-level `/-! ... -/` docstring explaining its contents and public surface.
  - rule: AGENTS.md PR-gate give-large-modules-a-docstring
  - evidence: `wc -l` = 895 lines. `rg -n '/-!' HigherOrder.lean` returns no matches (exit 1). File begins at line 1 with `import HansenEconometrics.Chapter10Bootstrap.PercentileT` then opens/namespace/variables and `section HigherOrderRefinements` — no docstring. `rg -c '^(theorem|lemma|def) '` = 19; `rg -c '^private '` = 0 (all public). AGENTS.md:18 = "Give large modules a docstring that explains their contents and public surface." Reference convention confirmed: `rg -n '/-!' DeltaMethod.lean` -> line 8; StochasticOrder.lean -> line 5. Note: finding's "27 public theorems" overstates the actual count of 19.
  - suggested fix: Add a `/-! # Chapter 10 higher-order bootstrap refinements ... -/` module docstring after the imports/opens summarizing the two public stacks (Theorem 10.15 percentile-t interval expansion and Theorem 10.17 abs-test rejection expansion) and the fixed / `_of_transfer` / `_event_` / `_polynomial` / `_one_sub_alpha` naming axes.
  - mechanical: false | confidence: medium
- **[nit]** `chapter10_percentileT_secondOrder_interval_event_expansion_polynomial_one_sub_alpha_of_transfer` (line 441) — Several theorem identifiers are 79-95 characters long and are so long the name must be wrapped onto its own line after the `theorem` keyword (lines 248, 340, 406, 441, 664, 761, 824, 859); this one at 95 chars is the longest and would be hard to cite inline.
  - rule: AGENTS.md PR-gate citeable-names-use-namespaces
  - evidence: AGENTS.md:16: "- Keep theorem names citeable; use namespaces and local names to avoid very long identifiers." File namespace markers: line 8 `namespace HansenEconometrics`, line 15 `section HigherOrderRefinements`, line 893 `end HigherOrderRefinements`, line 895 `end HansenEconometrics` (no sub-namespace). Read of lines 441-442 shows `theorem` alone on 441 and the name on 442. `echo -n "chapter10_percentileT_secondOrder_interval_event_expansion_polynomial_one_sub_alpha_of_transfer" | wc -c` = 95. sed of lines 248/340/406/441/664/761/824/859 each prints just `theorem`, confirming the recurring wrap-onto-next-line pattern.
  - suggested fix: Introduce a sub-namespace (e.g. `namespace HigherOrder` / `PercentileT` / `AbsTest`) so the local names can drop the `chapter10_percentileT_secondOrder_` prefix, shortening the citeable identifier to e.g. `interval_event_expansion_polynomial_one_sub_alpha_of_transfer`.
  - mechanical: false | confidence: low

### proof-quality

- **[nit]** `chapter10_abs_test_secondOrder_rejection_expansion_polynomial` (line 699) — The proof is an unnecessary `by exact <term>` tactic wrapper around a pure term-mode delegation; the `by`/`exact` adds no tactic work and can be dropped, matching the direct term-mode style used by every sibling polynomial wrapper.
  - rule: AGENTS.md Proof strategy policy (use Mathlib's abstraction level / direct proofs)
  - evidence: HigherOrder.lean line 715: `      atTop (𝓝 0) := by`; line 716: `  exact`; lines 717-724 delegate to `chapter10_abs_test_secondOrder_rejection_expansion ...`. Contrast sibling at line 813-814: `      atTop (𝓝 0) :=` immediately followed by `  chapter10_abs_test_secondOrder_rejection_expansion_one_sub_alpha ...` with no by/exact. Siblings at 750-751 and 785-786 are also bare term mode. AGENTS.md lines 273-275 state the proof strategy policy favoring direct proofs.
  - suggested fix: Replace `:= by\n  exact\n    chapter10_abs_test_secondOrder_rejection_expansion ...` with direct term mode `:=\n  chapter10_abs_test_secondOrder_rejection_expansion ...`, dropping the `by` and `exact` keywords.
  - mechanical: true | confidence: high

## HansenEconometrics/Chapter10Bootstrap/Quantiles.lean

### redundancy

- **[minor]** `bootstrapScalarCDF` (line 568) — bootstrapScalarCDF re-inlines the `(measure {Z ≤ x}).toReal` body of the already-defined scalarCDF instead of delegating to it, duplicating the same definitional algebra in the same file.
  - rule: AGENTS.md §2 reuse-repo-theorems-first
  - evidence: Quantiles.lean:319 `scalarCDF (ν : Measure Ωlim) (Z : Ωlim → ℝ) (x : ℝ) : ℝ := (ν {ωlim | Z ωlim ≤ x}).toReal`; line 11 `variable {Ω Ωs Ωlim ...}` (Ωlim is a free type var, so the def is generic in the sample space). Quantiles.lean:570-571 `bootstrapScalarCDF ... := ((Pstar n ω) {ωs | Zstar n ω ωs ≤ x}).toReal`. Proof duplication: scalarCDF_mono (327-328) `refine ENNReal.toReal_mono (measure_ne_top ν ...) ?_; exact measure_mono fun ωlim hωlim => le_trans hωlim hxy` vs bootstrapScalarCDF_mono (580-582) `refine ENNReal.toReal_mono (measure_ne_top (Pstar n ω) ...) ?_; exact measure_mono fun ωs hωs => le_trans hωs hxy` — identical modulo variable names. Usage check (fixer safety): `rg bootstrapScalarCDF` shows many external callers in Percentile.lean, PercentileT.lean, Tests.lean, but every usage is to the term/lemma, not its definitional body; redefining bootstrapScalarCDF as `scalarCDF (Pstar n ω) (Zstar n ω) x` is definitionally equal so all external callers remain valid. The suggested fix (delegate def to scalarCDF and prove mono via scalarCDF_mono) is therefore safe.
  - suggested fix: Define `bootstrapScalarCDF Pstar Zstar x n ω := scalarCDF (Pstar n ω) (Zstar n ω) x`, then prove bootstrapScalarCDF_mono via scalarCDF_mono instead of repeating the ENNReal.toReal_mono argument.
  - mechanical: false | confidence: high
- **[minor]** `bootstrapScalarCDFIndexed` (line 1302) — bootstrapScalarCDFIndexed re-inlines the same `(measure {Z ≤ x}).toReal` body as scalarCDF rather than delegating to it; the sample-size-dependent Ωboot type does not prevent reuse since Pstar n ω : Measure (Ωboot n) and Zstar n ω : Ωboot n → ℝ still match scalarCDF's signature.
  - rule: AGENTS.md §2 reuse-repo-theorems-first
  - evidence: Bodies are identical in shape: scalarCDF (line 320) `(ν {ωlim | Z ωlim ≤ x}).toReal`; bootstrapScalarCDF (line 571) `((Pstar n ω) {ωs | Zstar n ω ωs ≤ x}).toReal`; bootstrapScalarCDFIndexed (line 1306) `((Pstar n ω) {ωs | Zstar n ω ωs ≤ x}).toReal`. scalarCDF signature: `scalarCDF (ν : Measure Ωlim) (Z : Ωlim → ℝ) (x : ℝ)` with `{Ωlim : Type*}` implicit (declared line 11). Usage check: `rg -l 'bootstrapScalarCDFIndexed' --glob='*.lean'` returns ONLY Quantiles.lean — zero external callers, so delegating/removing the inlined body is safe. Build verification: applying the suggested fix `scalarCDF (Pstar n ω) (Zstar n ω) x`, `lake build HansenEconometrics.Chapter10Bootstrap.Quantiles` shows the definition itself type-checks (no error at line 1302). FIXER CAVEAT: the literal one-line fix produces two `unsolved goals` errors at line 1317 (bootstrapScalarCDFIndexed_eq_cdf_map) and line 1463 (the vector lemma) because those proofs end with `simp [bootstrapScalarCDFIndexed, ...]` relying on the inlined body; the fixer must add `scalarCDF` to those simp sets (e.g. `simp [bootstrapScalarCDFIndexed, scalarCDF, ...]`). File restored to clean HEAD state after testing (git status shows no tracked changes).
  - suggested fix: Define `bootstrapScalarCDFIndexed Pstar Zstar x n ω := scalarCDF (Pstar n ω) (Zstar n ω) x` to delegate to the canonical scalarCDF surface instead of re-inlining the toReal-of-measure algebra.
  - mechanical: false | confidence: medium
- **[nit]** `bootstrapScalarCDF_mono` (line 575) — bootstrapScalarCDF_mono reproduces verbatim the ENNReal.toReal_mono / measure_mono monotonicity proof of scalarCDF_mono rather than delegating, because bootstrapScalarCDF does not reuse scalarCDF.
  - rule: AGENTS.md §4 bridge-lemmas-delegate
  - evidence: bootstrapScalarCDF_mono (Quantiles.lean:579-582): `intro x y hxy; refine ENNReal.toReal_mono (measure_ne_top (Pstar n ω) {ωs | Zstar n ω ωs ≤ y}) ?_; exact measure_mono fun ωs hωs => le_trans hωs hxy`. scalarCDF_mono (Quantiles.lean:326-328): `intro x y hxy; refine ENNReal.toReal_mono (measure_ne_top ν {ωlim | Z ωlim ≤ y}) ?_; exact measure_mono fun ωlim hωlim => le_trans hωlim hxy`. Definitions: bootstrapScalarCDF (line 571) = `((Pstar n ω) {ωs | Zstar n ω ωs ≤ x}).toReal`; scalarCDF (line 320) = `(ν {ωlim | Z ωlim ≤ x}).toReal` — definitionally equal under ν:=Pstar n ω, Z:=Zstar n ω. Empirical test: patched body `exact scalarCDF_mono (ν := Pstar n ω) (Z := Zstar n ω)` produced no error at the lemma site; the only errors reported (lines 1314/1457 patched, 1317/1463 original) are pre-existing and unrelated. Usage check: `rg -l 'bootstrapScalarCDF_mono'` shows external callers in PercentileT.lean, Percentile.lean, Tests.lean, but the fix preserves the lemma (only swaps its proof body), so external callers are unaffected. Rule: AGENTS.md §4 "Use bridge lemmas instead of duplicating proof ideas".
  - suggested fix: Once bootstrapScalarCDF delegates to scalarCDF, replace this proof body with `exact scalarCDF_mono (ν := Pstar n ω) (Z := Zstar n ω)` (instance IsFiniteMeasure (Pstar n ω) is in scope).
  - mechanical: false | confidence: medium
- **[nit]** `bootstrapScalarCDFIndexed_mono` (line 1328) — bootstrapScalarCDFIndexed_mono reproduces verbatim the same ENNReal.toReal_mono / measure_mono monotonicity proof as scalarCDF_mono and bootstrapScalarCDF_mono rather than delegating to the canonical scalarCDF_mono.
  - rule: AGENTS.md §4 bridge-lemmas-delegate
  - evidence: Clean baseline (git-committed) compiles with zero errors. bootstrapScalarCDFIndexed_mono proof lines 1333-1336 == scalarCDF_mono lines 326-328 (only ωs vs ωlim differ). Test: replaced the proof with `scalarCDF_mono (ν := Pstar n ω) (Z := Zstar n ω)` -> `lake env lean HansenEconometrics/Chapter10Bootstrap/Quantiles.lean` returned no error/sorry/warning lines. The compiler also accepts `scalarCDF (Pstar n ω) (Zstar n ω) x` elsewhere in the file (observed goal state), confirming Ωlim unifies with Ωboot n. Then restored original tree state.
  - suggested fix: Once bootstrapScalarCDFIndexed delegates to scalarCDF, replace this proof with `exact scalarCDF_mono (ν := Pstar n ω) (Z := Zstar n ω)`.
  - mechanical: false | confidence: medium

### hygiene

- **[major]** `<module>` (line 9) — This 2114-line module exposes roughly 80 public declarations (quantile-convergence constructors, scalar/bootstrap CDF bridges, the indexed family) but has no module-level docstring explaining its contents or public surface.
  - rule: AGENTS.md PR-gate: give large modules a docstring
  - evidence: `wc -l` = 2113. `grep -n '/-!' Quantiles.lean` exits 1 (no match) — no module docstring marker anywhere. `rg -c '^(noncomputable )?(def|theorem|lemma|structure|abbrev|class|instance) '` = 71 declarations, of which only 1 is private (line 84: `private theorem stieltjesFunction_exists_right_lt_of_lt`), leaving ~70 public. File head (lines 1-9) goes from imports/opens directly to `namespace HansenEconometrics` with no `/-! -/` block. Cited decls confirmed present: `structure CDFQuantileBracket` (line 24), `theorem tendstoInMeasure_quantile_of_cdf_brackets` (line 130); `Indexed` appears 178 times. AGENTS.md:18: "Give large modules a docstring that explains their contents and public surface."
  - suggested fix: Add a `/-! # ... -/` module docstring after the imports/opens describing the quantile-convergence engine, the scalar/bootstrap CDF bridges, the standard-normal continuity lemmas, and the indexed (sample-size-dependent) mirror family, plus which declarations form the chapter-facing public API.
  - mechanical: false | confidence: high
- **[nit]** `bootstrapScalarCDFIndexed_eq_cdf_map` (line 1310) — This indexed CDF-bridge helper is public but is only used inside Quantiles.lean (unlike its non-indexed twin `bootstrapScalarCDF_eq_cdf_map`, no sibling chapter file references it), so it is locally-only proof scaffolding that should be `private`.
  - rule: AGENTS.md hygiene: keep same-file proof scaffolding private
  - evidence: `rg -l 'bootstrapScalarCDFIndexed_eq_cdf_map' --glob '*.lean'` -> only HansenEconometrics/Chapter10Bootstrap/Quantiles.lean. `rg -n` -> decl at 1310; uses at 1356, 1402, 1435 (all same file). `rg -n 'bootstrapScalarCDFIndexed_eq_cdf_map' -g '!Quantiles.lean'` -> exit 1 (no matches). Line 1310: `theorem bootstrapScalarCDFIndexed_eq_cdf_map` (no `private`). inventory/ch10-inventory.md:728 lists `bootstrapScalarCDF_eq_cdf_map` (twin) as public API but does NOT list `bootstrapScalarCDFIndexed_eq_cdf_map`. AGENTS.md:14 "Keep same-file proof scaffolding private."; AGENTS.md:98-99 "A helper whose removal would only break the file where it is defined should usually be `private`."; AGENTS.md:87-88 bridge `_of_...`/notation lemmas private "unless downstream files should cite them directly."
  - suggested fix: Mark `private theorem bootstrapScalarCDFIndexed_eq_cdf_map ...`, or document why the indexed CDF bridge should be a citeable public lemma if downstream files are expected to use it.
  - mechanical: false | confidence: medium
- **[nit]** `bootstrapScalarCDFIndexed_mono` (line 1328) — This indexed monotonicity helper is public but has zero cross-file usage, whereas its non-indexed twin `bootstrapScalarCDF_mono` is genuinely cited by Percentile/PercentileT/Tests; the indexed version is locally-only scaffolding that should be `private`.
  - rule: AGENTS.md hygiene: keep same-file proof scaffolding private
  - evidence: `rg -l 'bootstrapScalarCDFIndexed_mono' --glob '*.lean'` → only HansenEconometrics/Chapter10Bootstrap/Quantiles.lean. `rg -l 'bootstrapScalarCDF_mono'` → Quantiles.lean, Percentile.lean, Tests.lean, PercentileT.lean (4 files). In-file usage of the indexed helper: line 1328 (decl, plain `theorem`, not private), lines 1814/1890/2071 (callers, all in ..._finite / ..._id_cdf_finite wrappers). AGENTS.md:14 "Keep same-file proof scaffolding private."; AGENTS.md:98-99 "A helper whose removal would only break the file where it is defined should usually be `private`."
  - suggested fix: Mark `private theorem bootstrapScalarCDFIndexed_mono ...` unless an external file is expected to cite it.
  - mechanical: false | confidence: medium
- **[nit]** `bootstrapScalarCDFIndexed_exists_right_lt_of_lt` (line 1340) — This `_exists_right_lt_of_lt` right-continuity bridge for the indexed CDF is public but only used inside Quantiles.lean (by `bootstrapScalarCDFIndexed_local_right_lt_of_aemeasurable`), so it is proof infrastructure that should be `private`.
  - rule: AGENTS.md hygiene: make _of_ bridge lemmas private unless cited downstream
  - evidence: `rg -l 'bootstrapScalarCDFIndexed_exists_right_lt_of_lt' --glob '*.lean'` returns only `HansenEconometrics/Chapter10Bootstrap/Quantiles.lean`. In-file: line 1340 declares `theorem bootstrapScalarCDFIndexed_exists_right_lt_of_lt` (public), and the sole usage is at line 1376 inside `bootstrapScalarCDFIndexed_local_right_lt_of_aemeasurable` (defined at line 1366). AGENTS.md line 87: "`_of_...` bridge lemmas are usually proof infrastructure. Make them private unless downstream files should cite them directly." AGENTS.md lines 98-99: a helper whose removal would only break its own file should usually be `private`. The packager wrapper is itself also only in-file, but it is the chapter-facing API surface; the finding targets the underlying `_of_lt` infra lemma, which is correctly identified as needing `private`.
  - suggested fix: Mark `private theorem bootstrapScalarCDFIndexed_exists_right_lt_of_lt ...`.
  - mechanical: false | confidence: medium
- **[nit]** `bootstrapScalarCDFIndexed_local_right_lt_of_aemeasurable` (line 1366) — This indexed local-right-bracketing package is public but only used inside Quantiles.lean, unlike its cross-file-used non-indexed twin `bootstrapScalarCDF_local_right_lt_of_aemeasurable`; it is locally-only scaffolding that should be `private`.
  - rule: AGENTS.md hygiene: keep same-file proof scaffolding private
  - evidence: `rg -l 'bootstrapScalarCDFIndexed_local_right_lt_of_aemeasurable' --glob '*.lean'` -> only HansenEconometrics/Chapter10Bootstrap/Quantiles.lean. In-file occurrences: line 1366 (def, `theorem` not `private`), lines 1970 & 2077 (uses) inside `bootstrapScalarLowerQuantileIndexed_tendsto_of_bootstrapDistribution_unit_id_cdf_aemeasurable` (1939) and `..._law_cdf_probability` (2042). `rg -l 'bootstrapScalarCDF_local_right_lt_of_aemeasurable'` -> Quantiles.lean, Tests.lean, Percentile.lean, PercentileT.lean (3 sibling files). AGENTS.md:14 "Keep same-file proof scaffolding private."; AGENTS.md:87 "`_of_...` bridge lemmas are usually proof infrastructure. Make them private unless downstream files..."
  - suggested fix: Mark `private theorem bootstrapScalarCDFIndexed_local_right_lt_of_aemeasurable ...` unless a sibling file is expected to cite it.
  - mechanical: false | confidence: medium
- **[nit]** `bootstrapScalarCDFIndexed_level_nonempty_of_aemeasurable` (line 1382) — This indexed level-nonemptiness helper is public but has no cross-file usage, unlike its cross-file-cited non-indexed twin; it is locally-only proof scaffolding that should be `private`.
  - rule: AGENTS.md hygiene: keep same-file proof scaffolding private
  - evidence: `rg -l 'bootstrapScalarCDFIndexed_level_nonempty_of_aemeasurable' --glob '*.lean'` -> only HansenEconometrics/Chapter10Bootstrap/Quantiles.lean. `rg -l 'bootstrapScalarCDF_level_nonempty_of_aemeasurable'` -> Quantiles.lean, Tests.lean, Percentile.lean, PercentileT.lean (twin is cross-file used). In-file occurrences via `rg -n`: definition at line 1382 (`theorem bootstrapScalarCDFIndexed_level_nonempty_of_aemeasurable`, not preceded by `private`), and uses at lines 2030 and 2073. Lines 2026-2034 show the call inside `..._unit_id_cdf_aemeasurable`; lines 2065-2078 show the call inside `..._unit_law_cdf_probability`.
  - suggested fix: Mark `private theorem bootstrapScalarCDFIndexed_level_nonempty_of_aemeasurable ...`.
  - mechanical: false | confidence: medium
- **[nit]** `bootstrapScalarCDFIndexed_level_bddBelow_of_aemeasurable` (line 1411) — This indexed bounded-below helper is public but has no cross-file usage, unlike its cross-file-cited non-indexed twin; it is locally-only proof scaffolding that should be `private`.
  - rule: AGENTS.md hygiene: keep same-file proof scaffolding private
  - evidence: `rg -l 'bootstrapScalarCDFIndexed_level_bddBelow_of_aemeasurable' --glob '*.lean'` → only HansenEconometrics/Chapter10Bootstrap/Quantiles.lean. In-file refs: line 1411 (decl: `theorem bootstrapScalarCDFIndexed_level_bddBelow_of_aemeasurable`, not private), line 2032 (inside `bootstrapScalarLowerQuantileIndexed_tendsto_of_bootstrapDistribution_unit_id_cdf_*`), line 2075 (inside `...unit_law_cdf_probability`). Contrast: `rg -l 'bootstrapScalarCDF_level_bddBelow_of_aemeasurable' --glob '*.lean'` → Quantiles.lean, Tests.lean, Percentile.lean, PercentileT.lean (3 sibling files), so the non-indexed twin legitimately stays public.
  - suggested fix: Mark `private theorem bootstrapScalarCDFIndexed_level_bddBelow_of_aemeasurable ...`.
  - mechanical: false | confidence: medium
- **[nit]** `bootstrapScalarLowerQuantile_tendsto_of_bootstrapDistribution_unit_strictMono_id_cdf_aemeasurable` (line 1172) — Several lower-quantile wrapper theorem names reach 90-98 characters by concatenating 5-6 qualifiers, so long that the `theorem` keyword had to be placed on its own line; this one (97 chars) typifies a flat-naming pattern that is hard to cite in prose.
  - rule: AGENTS.md PR-gate: keep theorem names citeable; use namespaces/local names to avoid very long identifiers
  - evidence: Line 1172 `theorem` alone, line 1173 name = 97 chars (measured). AGENTS.md:16 = "Keep theorem names citeable; use namespaces and local names to avoid very long identifiers." Family confirmed via Read: line 1276 `..._strictMono_id_cdf_probability` (96), line 1858 `...Indexed..._unit_id_cdf_finite` (87), line 1938/1939 `...Indexed..._id_cdf_aemeasurable` (93), line 2041/2042 `...Indexed..._law_cdf_probability` (93) — all with `theorem` on its own line. (Note: finding mislabeled line 1858 as 98 chars; it is actually 87 and 1939 is 93 — minor numbering imprecision that does not affect the confirmed long-name pattern.) Namespace scope: only `namespace HansenEconometrics` (line 9) wraps these; `QuantileConvergence` at line 16 is a `section`, not a namespace.
  - suggested fix: Group these convergence wrappers under a dedicated namespace (e.g. `namespace BootstrapScalarLowerQuantile`) so the per-theorem suffixes (`tendsto_of_bootstrapDistribution_unit_strictMono_id_cdf_aemeasurable`) become shorter local names, or otherwise shorten the qualifier chain.
  - mechanical: false | confidence: medium

## Draft fixes

1 mechanical finding(s) sent to 1 fixer worktree(s). Per-file outcomes:

```
The target file `HansenEconometrics/Chapter10Bootstrap/HigherOrder.lean` does not exist anywhere in this worktree. Combined with the fact that the requested edit (dropping `by`/`exact`) is a forbidden proof-body change, this finding cannot be applied.

[{"status":"report_only","decl":"chapter10_abs_test_secondOrder_rejection_expansion_polynomial","reason":"forbidden edit type — dropping the `by`/`exact` wrapper is a proof-body change (proof restructuring), which is explicitly forbidden and maps to none of the allowed edit types (make_private|add_docstring|add_simp|in_file_rename|remove_duplicate). Additionally, the target file HansenEconometrics/Chapter10Bootstrap/HigherOrder.lean does not exist in this worktree (no Chapter10Bootstrap directory and no HigherOrder.lean found), so the edit could not be located or applied. No file was modified and lake build was not run.","build_error":null}]
```
